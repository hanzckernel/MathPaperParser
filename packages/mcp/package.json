{
  "name": "@paperparser/mcp",
  "private": true,
  "version": "0.2.0-alpha.2",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run build --workspace @paperparser/core && tsc -p tsconfig.json",
    "typecheck": "npm run build --workspace @paperparser/core && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@paperparser/core": "file:../core"
  }
}
