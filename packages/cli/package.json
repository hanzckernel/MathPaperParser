{
  "name": "@paperparser/cli",
  "private": true,
  "version": "0.2.0-alpha.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run build --workspace @paperparser/core && npm run build --workspace @paperparser/mcp && tsc -p tsconfig.json",
    "typecheck": "npm run build --workspace @paperparser/core && npm run build --workspace @paperparser/mcp && tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@paperparser/core": "file:../core",
    "@paperparser/mcp": "file:../mcp"
  }
}
