---
phase: 03-deterministic-dependency-explorer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/proof-graph-page.tsx
  - packages/web/test/proof-graph-render.test.ts
  - packages/web/test/bundle-data.test.ts
autonomous: true
requirements:
  - EXPL-01
  - EXPL-02
  - EXPL-03
must_haves:
  truths:
    - The local explorer keeps working from the canonical bundle without adding a second UI workflow.
    - Dependency inspection is node-centered but relation-aware: the user can inspect a node and then inspect one of its edges deliberately.
    - A selected edge exposes provenance, evidence, and explanation text in the UI.
  artifacts:
    - path: packages/web/src/components/proof-graph-page.tsx
      provides: Deterministic dependency inspection and edge explanation UI
    - path: packages/web/test/proof-graph-render.test.ts
      provides: Render regression for dependency and edge explanation behavior
  key_links:
    - from: packages/web/src/components/proof-graph-page.tsx
      to: packages/web/test/proof-graph-render.test.ts
      via: selected node and selected edge render states
      pattern: Edge Explanation
    - from: packages/web/src/components/proof-graph-page.tsx
      to: packages/web/src/lib/dashboard-model.ts
      via: rendered edge detail from serialized bundle fields
      pattern: provenance
---

<objective>
Finish the deterministic dependency explorer by making relation inspection first-class in the existing graph route.

Purpose: satisfy the explorer milestone without rebuilding the app shell or changing the canonical artifact.
Output: Graph detail UI that shows node dependencies plus a structured explanation panel for the selected edge.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-deterministic-dependency-explorer/03-CONTEXT.md
@.planning/phases/03-deterministic-dependency-explorer/03-RESEARCH.md
@.planning/phases/03-deterministic-dependency-explorer/03-VALIDATION.md
@packages/web/src/App.tsx
@packages/web/src/components/proof-graph-page.tsx
@packages/web/src/lib/dashboard-model.ts
@packages/web/test/proof-graph-render.test.ts
@packages/web/test/bundle-data.test.ts

Locked decisions to honor in this phase:
- Keep the work inside the current graph route and existing local explorer shell.
- Do not add search or provenance-filter work for agent-inferred edges; that belongs to later phases.
- Use the deterministic canonical edge fields directly in the explanation UI.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add first-class edge inspection to the graph detail surface</name>
  <files>packages/web/src/components/proof-graph-page.tsx, packages/web/test/proof-graph-render.test.ts, packages/web/test/bundle-data.test.ts</files>
  <action>Keep the existing selected-node workflow, but add selected-edge state within `GraphPage`. The Uses / Used By lists should let the user inspect a relation directly and still offer navigation to the adjacent node. Add a structured explanation panel that shows source node, target node, kind, provenance, evidence, detail, and any meaningful metadata such as `latexRef`, `latexCommand`, or `citeKey`. Keep existing search/filter behavior intact and do not redesign unrelated routes.</action>
  <verify>
    <automated>npx vitest run packages/web/test/proof-graph-render.test.ts packages/web/test/bundle-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A selected node still shows its dependencies and dependents.
    - At least one visible relation can be selected and rendered as a structured explanation.
    - The render tests prove the explanation panel includes provenance, evidence, and detail.
  </acceptance_criteria>
  <done>Phase 3 explorer requirements are satisfied on the existing graph route.</done>
</task>

</tasks>

<verification>
<automated>npx vitest run packages/web/test/proof-graph-render.test.ts packages/web/test/bundle-data.test.ts</automated>
<automated>npm test</automated>
<automated>npm run typecheck</automated>
</verification>

<success_criteria>
Plan 03-01 is complete when the local explorer lets a mathematician inspect a node’s dependencies and inspect a selected deterministic relation with a structured why-this-edge-exists explanation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-deterministic-dependency-explorer/03-01-SUMMARY.md`
</output>
