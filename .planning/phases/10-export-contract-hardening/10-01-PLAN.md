---
phase: 10-export-contract-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/export.ts
  - packages/cli/src/store.ts
  - packages/cli/test/export-command.test.ts
  - packages/web/test/data-source.test.ts
autonomous: true
requirements:
  - EXPORT-01
  - EXPORT-02
must_haves:
  truths:
    - Static export must resolve `--paper latest` from the explicit store pointer rather than a fallback heuristic.
    - Static export must always emit the full `data/` payload, including explicit `enrichment.json`.
    - Re-exporting into an existing output path must not leave stale artifacts behind.
  artifacts:
    - path: packages/cli/src/store.ts
      provides: Strict latest-paper resolution via `latest.json`
    - path: packages/cli/src/export.ts
      provides: Deterministic export output and replacement behavior for existing output directories
    - path: packages/cli/test/export-command.test.ts
      provides: CLI boundary proof for latest resolution, explicit enrichment nulls, and stale-file replacement
    - path: packages/web/test/data-source.test.ts
      provides: Static loader compatibility with explicit `enrichment.json = null`
  key_links:
    - from: packages/cli/src/store.ts
      to: packages/cli/src/export.ts
      via: strict `latest` pointer resolution before export build/write steps
      pattern: `--paper latest` returns the store pointer target or fails explicitly
    - from: packages/cli/src/export.ts
      to: packages/cli/test/export-command.test.ts
      via: explicit `enrichment.json` writing and output-directory replacement
      pattern: no stale files survive after re-export
    - from: packages/cli/src/export.ts
      to: packages/web/src/lib/data-source.ts
      via: exported `data/` payload compatibility
      pattern: static loader tolerates explicit `null` enrichment artifact
---

<objective>
Finish Phase 10 by hardening the existing CLI export contract without creating a new export surface.

Purpose: make static exports deterministic at the data-boundary layer before dashboard runtime work begins.
Output: strict latest-paper selection, complete exported artifacts, deterministic replacement of existing output paths, and automated compatibility proof at the CLI and static-loader boundaries.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-export-contract-hardening/10-CONTEXT.md
@packages/cli/src/export.ts
@packages/cli/src/store.ts
@packages/cli/src/index.ts
@packages/cli/test/export-command.test.ts
@packages/web/src/lib/data-source.ts
@packages/web/test/data-source.test.ts

Locked decisions to honor in this phase:
- `--paper latest` means the explicit store pointer only.
- Static exports always write `data/enrichment.json`; absence is represented by JSON `null`.
- Existing output directories are a deterministic replacement path, not a merge path.
- The acceptance bar is automated boundary tests only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the replacement behavior in a failing CLI export regression test</name>
  <files>packages/cli/test/export-command.test.ts</files>
  <action>Add a failing regression proving that exporting into an existing output directory removes stale files instead of merging with prior export contents.</action>
  <verify>
    <automated>npm test -- packages/cli/test/export-command.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The new regression fails on the current export behavior for the intended reason.
    - The test keeps Phase 10's existing latest/enrichment coverage intact.
  </acceptance_criteria>
  <done>The stale-output risk is captured as an executable regression.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make export deterministic for existing output paths</name>
  <files>packages/cli/src/export.ts</files>
  <action>Build the static dashboard into a temporary location, write the bundle payload there, then replace the target output directory so stale files cannot survive across exports.</action>
  <verify>
    <automated>npm test -- packages/cli/test/export-command.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Export still succeeds for Markdown and TeX fixtures.
    - `--paper latest` and explicit `enrichment.json = null` remain intact.
    - Re-exporting into an existing output path removes stale files.
  </acceptance_criteria>
  <done>The CLI export path behaves as a deterministic replace operation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove static loader compatibility with explicit null enrichment</name>
  <files>packages/web/test/data-source.test.ts</files>
  <action>Add a compatibility test showing that the static dashboard loader treats `enrichment.json` with explicit JSON `null` as an absent optional sidecar rather than as a crash or malformed artifact.</action>
  <verify>
    <automated>npm test -- packages/web/test/data-source.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Static-mode loader accepts explicit null enrichment without surfacing an enrichment object.
    - Existing static/API enrichment tests remain green.
  </acceptance_criteria>
  <done>The tightened export contract is proven compatible with the current static loader.</done>
</task>

</tasks>

<verification>
<automated>npm test -- packages/cli/test/export-command.test.ts</automated>
<automated>npm test -- packages/web/test/data-source.test.ts</automated>
<automated>npm run typecheck</automated>
</verification>

<success_criteria>
Plan 10-01 is complete when `--paper latest` is strict, static exports always write the full expected `data/` payload, re-exporting cannot leave stale artifacts behind, and automated tests prove the export and static-loader boundaries remain compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/10-export-contract-hardening/10-01-SUMMARY.md`
</output>
