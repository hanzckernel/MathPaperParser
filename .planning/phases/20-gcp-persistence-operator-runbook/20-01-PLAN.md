---
phase: 20-gcp-persistence-operator-runbook
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/cloudrun/deploy.sh
  - deploy/cloudrun/rollback.sh
  - deploy/cloudrun/RUNBOOK.md
  - deploy/cloudrun/README.md
  - packages/cli/test/cloud-run-runbook-contract.test.ts
  - README.md
  - docs/deployment_readiness.md
autonomous: true
requirements:
  - STORE-01
  - OPS-02
must_haves:
  truths:
    - The supported Cloud Run path must have one explicit persistence bridge for the current store contract.
    - Operators must be able to build, deploy, verify, upgrade, and roll back from repo-owned docs and helpers.
    - The runbook must call out the Cloud Storage FUSE limitations honestly.
  artifacts:
    - path: deploy/cloudrun/deploy.sh
      provides: mounted-bucket persistence contract
    - path: deploy/cloudrun/rollback.sh
      provides: explicit rollback command path
    - path: deploy/cloudrun/RUNBOOK.md
      provides: repo-backed operator workflow
    - path: packages/cli/test/cloud-run-runbook-contract.test.ts
      provides: local proof for the bucket mount and rollback helper
  key_links:
    - from: deploy/cloudrun/deploy.sh
      to: deploy/cloudrun/RUNBOOK.md
      via: mounted bucket contract
      pattern: the runbook and helper agree on the store path and required bucket/service-account inputs
    - from: deploy/cloudrun/rollback.sh
      to: deploy/cloudrun/RUNBOOK.md
      via: rollback procedure
      pattern: rollback is a repo-visible standard Cloud Run traffic command
---

<objective>
Finish Phase 20 by adding one supported Cloud Run persistence bridge and a repo-backed operator runbook around the new deployment/security path.

Purpose: make the Cloud Run path deployable and recoverable by an operator without hidden workstation knowledge.
Output: bucket-mounted deploy helper, rollback helper, runbook, and mocked contract proof.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-gcp-persistence-operator-runbook/20-CONTEXT.md
@.planning/phases/20-gcp-persistence-operator-runbook/20-RESEARCH.md
@deploy/cloudrun/deploy.sh
@deploy/cloudrun/grant-invoker.sh
@deploy/cloudrun/README.md
@README.md
@docs/deployment_readiness.md

Locked decisions to honor in this phase:
- Use a dedicated Cloud Storage bucket mounted into `/var/paperparser/store`.
- Keep the existing filesystem-backed store contract.
- Be explicit about Cloud Storage FUSE consistency and concurrency limitations.
- Cover build/push, deploy, verify, upgrade, and rollback from repo docs.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the persistence and rollback contract in failing mocked-script tests</name>
  <files>packages/cli/test/cloud-run-runbook-contract.test.ts</files>
  <action>Add mocked `gcloud` regressions for the deploy helper's bucket volume mount and the rollback helper's traffic command before implementation.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-run-runbook-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - At least one new runbook/persistence regression fails before implementation.
    - Tests pin the mount path and rollback command shape.
  </acceptance_criteria>
  <done>The persistence and rollback contract is executable before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the mounted-bucket deploy path and rollback helper</name>
  <files>deploy/cloudrun/deploy.sh, deploy/cloudrun/rollback.sh, deploy/cloudrun/README.md</files>
  <action>Extend the deploy helper to mount the Cloud Storage bucket at the supported store path, add a rollback helper, and update the deployment notes to match.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-run-runbook-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The deploy helper mounts the bucket at `/var/paperparser/store`.
    - The rollback helper uses a deterministic Cloud Run revision traffic command.
    - The shared-access notes stay aligned with the new persistence contract.
  </acceptance_criteria>
  <done>The deploy helpers now include the supported persistence and rollback surfaces.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Publish the operator runbook and re-prove the repo</name>
  <files>deploy/cloudrun/RUNBOOK.md, README.md, docs/deployment_readiness.md</files>
  <action>Write the repo-backed operator runbook covering build/push, bucket setup, deploy, invoker grants, verify, upgrade, and rollback, then rerun the mocked script tests and typecheck.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-run-runbook-contract.test.ts packages/cli/test/cloud-run-security-contract.test.ts</automated>
    <automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Operators can follow repo docs without hidden machine-specific steps.
    - The runbook states the supported bucket/service-account requirements and FUSE limitations explicitly.
    - The repo remains typecheck-green.
  </acceptance_criteria>
  <done>The Cloud Run path is supportable by an operator from repo-owned guidance.</done>
</task>

</tasks>

<verification>
<automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-run-runbook-contract.test.ts packages/cli/test/cloud-run-security-contract.test.ts</automated>
<automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
</verification>

<success_criteria>
Plan 20-01 is complete when the repo ships a mounted-bucket persistence contract, a rollback helper, and a repo-backed operator runbook that covers build/push through rollback, with mocked script tests and typecheck proving the path.
</success_criteria>

<output>
After completion, create `.planning/phases/20-gcp-persistence-operator-runbook/20-01-SUMMARY.md`
</output>
