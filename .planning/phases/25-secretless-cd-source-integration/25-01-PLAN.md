---
phase: 25-secretless-cd-source-integration
plan: "01"
type: execute
wave: 1
depends_on:
  - "24-01"
files_modified:
  - cloudbuild.release.yaml
  - deploy/cloudrun/bootstrap.sh
  - deploy/cloudrun/deploy-from-image-ref.sh
  - deploy/cloudrun/release-metadata.sh
  - deploy/cloudrun/sync-github-trigger.sh
  - deploy/cloudrun/RUNBOOK.md
  - deploy/cloudrun/README.md
  - deploy/cloudrun/SMOKE.md
  - README.md
  - packages/cli/test/cloud-build-pipeline-contract.test.ts
  - packages/cli/test/cloud-run-bootstrap-contract.test.ts
  - packages/cli/test/cloud-run-runbook-contract.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - PIPE-03
  - PIPE-04
  - GCP-03
must_haves:
  truths:
    - Phase 25 must keep Cloud Build as the deploy engine and connect it to GitHub as the hosted source of truth.
    - Automated deploys must use a dedicated user-specified Cloud Build service account instead of key-file secrets or the legacy default service account.
    - The release pipeline must deploy the exact digest-backed image produced in Phase 24 and surface deployed service metadata for later live smoke.
    - The automated path must preserve the same authenticated same-origin Cloud Run service and mounted bucket contract from `v1.4`.
  artifacts:
    - path: deploy/cloudrun/bootstrap.sh
      provides: CI/CD bootstrap for a bounded Cloud Build service account and IAM wiring
    - path: deploy/cloudrun/sync-github-trigger.sh
      provides: reproducible GitHub-to-Cloud-Build trigger wiring on `main`
    - path: cloudbuild.release.yaml
      provides: publish-plus-deploy contract using the exact pushed image digest
    - path: deploy/cloudrun/deploy-from-image-ref.sh
      provides: digest-based deploy handoff from build artifacts
    - path: deploy/cloudrun/release-metadata.sh
      provides: machine-readable deployed revision and image metadata
    - path: packages/cli/test/cloud-build-pipeline-contract.test.ts
      provides: local contract proof for trigger wiring and digest-to-deploy handoff
  key_links:
    - from: deploy/cloudrun/bootstrap.sh
      to: deploy/cloudrun/sync-github-trigger.sh
      via: shared Cloud Build service account
      pattern: trigger creation must reuse the bounded build identity created during bootstrap
    - from: cloudbuild.release.yaml
      to: deploy/cloudrun/deploy-from-image-ref.sh
      via: exact image handoff
      pattern: release deploy must consume the digest-backed image ref produced by resolve-image-digest
    - from: cloudbuild.release.yaml
      to: deploy/cloudrun/release-metadata.sh
      via: deployed revision handoff
      pattern: the hosted pipeline must emit machine-usable release metadata for later smoke and rollback steps
---

<objective>
Finish Phase 25 by turning the local Cloud Build release contract into a real GitHub-driven, secretless CD path that deploys the long-lived Cloud Run service without weakening the shipped security and runtime contract.

Purpose: connect GitHub `main` to Cloud Build, run deploys under a bounded build service account, and hand Phase 26 exact revision/image metadata after automated deploy.
Output: bootstrap and trigger helpers, release pipeline deploy handoff, docs alignment, contract tests, and updated milestone state.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/25-secretless-cd-source-integration/25-CONTEXT.md
@cloudbuild.release.yaml
@deploy/cloudrun/bootstrap.sh
@deploy/cloudrun/deploy.sh
@deploy/cloudrun/service-metadata.sh
@deploy/cloudrun/RUNBOOK.md
@deploy/cloudrun/README.md
@deploy/cloudrun/SMOKE.md
@README.md
@packages/cli/test/cloud-build-pipeline-contract.test.ts
@packages/cli/test/cloud-run-bootstrap-contract.test.ts
@packages/cli/test/cloud-run-runbook-contract.test.ts
@packages/cli/test/cloud-run-security-contract.test.ts

Locked decisions to honor in this phase:
- GitHub is the hosted source of truth and Cloud Build remains the deployment engine.
- Mainline updates should publish and deploy automatically to the long-lived Cloud Run service.
- The build/deploy path must run under a bounded user-specified Cloud Build service account with no key JSON.
- The deployed contract remains same-origin, authenticated, and mounted-store backed.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the Phase 25 hosted trigger and deploy handoff in tests</name>
  <files>packages/cli/test/cloud-build-pipeline-contract.test.ts, packages/cli/test/cloud-run-bootstrap-contract.test.ts, packages/cli/test/cloud-run-runbook-contract.test.ts</files>
  <action>Add failing regressions for the bounded build service account bootstrap, GitHub trigger sync helper, release-pipeline deploy handoff, and machine-readable deployed metadata contract.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-build-pipeline-contract.test.ts packages/cli/test/cloud-run-bootstrap-contract.test.ts packages/cli/test/cloud-run-runbook-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests fail before the new helpers and release-pipeline wiring exist.
    - Tests pin GitHub `main` trigger wiring, user-specified Cloud Build service account usage, digest-based deploy handoff, and release metadata output.
  </acceptance_criteria>
  <done>The Phase 25 contract is explicit before implementation starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded bootstrap, GitHub trigger sync, and release deploy handoff</name>
  <files>cloudbuild.release.yaml, deploy/cloudrun/bootstrap.sh, deploy/cloudrun/deploy-from-image-ref.sh, deploy/cloudrun/release-metadata.sh, deploy/cloudrun/sync-github-trigger.sh</files>
  <action>Extend the repo-owned GCP helpers to create the dedicated build service account and IAM bindings, create or update the GitHub `main` trigger, and deploy from the exact digest-backed image while surfacing deployed metadata.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-build-pipeline-contract.test.ts packages/cli/test/cloud-run-bootstrap-contract.test.ts packages/cli/test/cloud-run-runbook-contract.test.ts packages/cli/test/cloud-run-security-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Trigger wiring is reproducible from checked-in scripts and points at GitHub `main`.
    - The release pipeline uses a user-specified Cloud Build service account and does not rely on key JSON.
    - Automated deploy consumes the exact digest-backed image ref and emits revision/image metadata for downstream smoke.
  </acceptance_criteria>
  <done>The repo owns a real hosted source integration and secretless deploy path.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Align docs, provision the real trigger, and verify the hosted contract</name>
  <files>deploy/cloudrun/RUNBOOK.md, deploy/cloudrun/README.md, deploy/cloudrun/SMOKE.md, README.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Document the GitHub-driven Cloud Build path, provision the real build service account and trigger in GCP, and verify that the hosted contract preserves authenticated same-origin Cloud Run behavior.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm run ci:cloudbuild:release</automated>
    <automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
    <automated>real gcloud bootstrap and trigger sync in project paperparser-492322</automated>
  </verify>
  <acceptance_criteria>
    - Docs explain how GitHub `main` connects to Cloud Build and which service account runs build/deploy.
    - The live project has the bounded build service account and GitHub trigger created or updated from repo-owned helpers.
    - The maintained requirements/state surface records GCP-03, PIPE-03, and PIPE-04 progress accurately for the next phase.
  </acceptance_criteria>
  <done>The hosted CI/CD entry point is real, documented, and ready for live smoke.</done>
</task>

</tasks>

<verification>
<automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-build-pipeline-contract.test.ts packages/cli/test/cloud-run-bootstrap-contract.test.ts packages/cli/test/cloud-run-runbook-contract.test.ts packages/cli/test/cloud-run-security-contract.test.ts</automated>
<automated>PATH=/opt/homebrew/bin:$PATH npm run ci:cloudbuild:release</automated>
<automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
<automated>gcloud-backed bootstrap and trigger sync in project paperparser-492322</automated>
</verification>

<success_criteria>
Plan 25-01 is complete when the repo can provision a bounded Cloud Build service account and GitHub trigger from checked-in helpers, the release pipeline deploys the exact pushed image digest to the authenticated Cloud Run service, docs explain the hosted automation path, and the local contract tests plus live GCP trigger wiring succeed.
</success_criteria>

<output>
After completion, create `.planning/phases/25-secretless-cd-source-integration/25-01-SUMMARY.md`
</output>
