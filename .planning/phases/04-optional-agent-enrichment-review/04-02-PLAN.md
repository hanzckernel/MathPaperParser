---
phase: 04-optional-agent-enrichment-review
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - packages/web/src/lib/data-source.ts
  - packages/web/src/lib/dashboard-model.ts
  - packages/web/src/components/proof-graph-page.tsx
  - packages/web/src/App.tsx
  - packages/web/src/lib/api-client.ts
  - packages/web/test/
autonomous: true
requirements:
  - EXPL-04
  - ENRICH-03
must_haves:
  truths:
    - Deterministic edges remain the default explorer view.
    - Agent-inferred edges become visible only through explicit provenance opt-in.
    - Edge explanation stays evidence-first and gains confidence/provider review metadata for agent-inferred candidates.
  artifacts:
    - path: packages/web/src/lib/data-source.ts
      provides: Optional enrichment loading in static and API modes
    - path: packages/web/src/lib/dashboard-model.ts
      provides: Merged deterministic plus enrichment edge view
    - path: packages/web/src/components/proof-graph-page.tsx
      provides: Provenance toggles and enrichment-aware edge explanation
  key_links:
    - from: packages/web/src/lib/data-source.ts
      to: packages/web/test/data-source.test.ts
      via: optional enrichment fetch
      pattern: enrichment.json
    - from: packages/web/src/components/proof-graph-page.tsx
      to: packages/web/test/proof-graph-render.test.ts
      via: provenance toggle and confidence rendering
      pattern: agent_inferred
---

<objective>
Complete the explorer integration for the optional enrichment layer.

Purpose: let a mathematician review agent-inferred candidates without mixing them into the deterministic default view.
Output: enrichment-aware loading, provenance filtering, and review metadata in the graph route.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-optional-agent-enrichment-review/04-CONTEXT.md
@.planning/phases/04-optional-agent-enrichment-review/04-RESEARCH.md
@.planning/phases/04-optional-agent-enrichment-review/04-VALIDATION.md
@packages/web/src/lib/data-source.ts
@packages/web/src/lib/dashboard-model.ts
@packages/web/src/components/proof-graph-page.tsx
@packages/web/src/App.tsx
@packages/web/src/lib/api-client.ts
@packages/web/test/

Locked decisions to honor in this plan:
- The explorer loads enrichment opportunistically; absence of `enrichment.json` is not an error.
- Provenance filtering replaces evidence-only trust filtering for graph visibility.
- Agent-inferred edges show confidence and provider/review metadata in the existing explanation surface.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Load optional enrichment and merge it into the dashboard model</name>
  <files>packages/web/src/lib/data-source.ts, packages/web/src/lib/dashboard-model.ts, packages/web/src/App.tsx, packages/web/test/data-source.test.ts, packages/web/test/bundle-data.test.ts</files>
  <action>Extend the web data-loading path so static and API modes attempt to load `enrichment.json` or `/enrichment` without failing when the sidecar is absent. Merge candidate edges into the dashboard model in a way that preserves deterministic nodes and keeps provenance metadata intact.</action>
  <verify>
    <automated>npx vitest run packages/web/test/data-source.test.ts packages/web/test/bundle-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The web app still loads pure deterministic bundles.
    - When an enrichment sidecar is present, agent-inferred edges are available in the dashboard model with confidence metadata preserved.
  </acceptance_criteria>
  <done>The explorer data model is enrichment-aware without breaking deterministic-only loads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add provenance-layer controls and enrichment review detail to the graph page</name>
  <files>packages/web/src/components/proof-graph-page.tsx, packages/web/src/lib/api-client.ts, packages/web/test/proof-graph-render.test.ts, packages/web/test/api-client.test.ts</files>
  <action>Replace the graph trust filter with provenance toggles that default to `explicit` and `structural` only. Keep the existing explanation surface, but show confidence and provider/review metadata when the selected edge is agent-inferred. Do not surface agent edges unless the user enables their provenance class.</action>
  <verify>
    <automated>npx vitest run packages/web/test/proof-graph-render.test.ts packages/web/test/api-client.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Deterministic edges are visible by default and agent-inferred edges are hidden by default.
    - The UI can explicitly enable `agent_inferred` edges.
    - Selecting an agent-inferred edge shows confidence and review metadata.
  </acceptance_criteria>
  <done>The graph route satisfies EXPL-04 and completes the review surface for Phase 4.</done>
</task>

</tasks>

<verification>
<automated>npx vitest run packages/web/test/data-source.test.ts packages/web/test/bundle-data.test.ts packages/web/test/proof-graph-render.test.ts packages/web/test/api-client.test.ts</automated>
</verification>

<success_criteria>
Plan 04-02 is complete when the explorer loads optional enrichment sidecars, defaults to deterministic provenance, and lets the user inspect agent-inferred candidates deliberately.
</success_criteria>

<output>
After completion, create `.planning/phases/04-optional-agent-enrichment-review/04-02-SUMMARY.md`
</output>
