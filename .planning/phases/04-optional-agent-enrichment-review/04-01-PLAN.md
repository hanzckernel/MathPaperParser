---
phase: 04-optional-agent-enrichment-review
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/types/enrichment.ts
  - packages/core/src/enrichment/
  - packages/core/src/validation/schema-validator.ts
  - packages/core/src/validation/consistency-checker.ts
  - packages/cli/src/store.ts
  - packages/cli/src/index.ts
  - packages/cli/src/export.ts
  - packages/cli/src/server.ts
  - packages/mcp/src/server.ts
  - schema/enrichment.schema.json
  - packages/core/test/
  - packages/cli/test/
  - packages/mcp/test/server.test.ts
autonomous: true
requirements:
  - ENRICH-01
  - ENRICH-02
  - ENRICH-03
must_haves:
  truths:
    - The deterministic canonical artifact stays untouched and remains the only source of truth for the base parse.
    - Agent-inferred candidates live in a separate `enrichment.json` sidecar tied to stable node IDs.
    - The enrichment path is runnable in this repo without requiring an external model service.
  artifacts:
    - path: packages/core/src/types/enrichment.ts
      provides: Typed enrichment sidecar contract
    - path: schema/enrichment.schema.json
      provides: Validation contract for stored enrichment artifacts
    - path: packages/cli/src/index.ts
      provides: Opt-in `enrich` command for stored papers
    - path: packages/cli/src/server.ts
      provides: HTTP route for the enrichment sidecar
    - path: packages/mcp/src/server.ts
      provides: Real MCP enrichment resource
  key_links:
    - from: packages/cli/src/index.ts
      to: packages/cli/test/enrich-command.test.ts
      via: second-pass enrichment command
      pattern: enrichment.json
    - from: packages/cli/src/server.ts
      to: packages/cli/test/serve-app.test.ts
      via: served enrichment resource
      pattern: /enrichment
    - from: packages/mcp/src/server.ts
      to: packages/mcp/test/server.test.ts
      via: MCP enrichment resource
      pattern: paperparser://papers
---

<objective>
Create the separate enrichment artifact and generation path for Phase 4.

Purpose: make second-pass review candidates real without mutating the canonical bundle.
Output: stored `enrichment.json`, generation service, CLI command, export/server/MCP exposure, and regression tests.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-optional-agent-enrichment-review/04-CONTEXT.md
@.planning/phases/04-optional-agent-enrichment-review/04-RESEARCH.md
@.planning/phases/04-optional-agent-enrichment-review/04-VALIDATION.md
@packages/core/src/types/edge.ts
@packages/core/src/serialization/bundle-serializer.ts
@packages/core/src/validation/schema-validator.ts
@packages/core/src/validation/consistency-checker.ts
@packages/cli/src/store.ts
@packages/cli/src/index.ts
@packages/cli/src/export.ts
@packages/cli/src/server.ts
@packages/mcp/src/server.ts

Locked decisions to honor in this plan:
- Enrichment is a sidecar, not an addition to `graph.json`.
- The built-in provider must be honest about being a local heuristic reviewer, not a hidden remote model.
- Every candidate edge must carry confidence plus explanation metadata and use `agent_inferred` provenance.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the enrichment sidecar contract, validation, and generation service</name>
  <files>packages/core/src/types/enrichment.ts, packages/core/src/enrichment/, packages/core/src/validation/schema-validator.ts, packages/core/src/validation/consistency-checker.ts, schema/enrichment.schema.json, packages/core/test/</files>
  <action>Create a typed enrichment artifact that records provider metadata, base-paper linkage, and candidate edges keyed to existing node IDs. Add validation for the sidecar, and implement a deterministic built-in reviewer that proposes `agent_inferred` candidate edges with confidence plus support metadata from the existing bundle. Keep the canonical bundle unchanged.</action>
  <verify>
    <automated>npx vitest run packages/core/test/enrichment-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The enrichment artifact validates and references only existing node IDs.
    - Generated candidate edges carry `agent_inferred` provenance, confidence, and explanation metadata.
    - The generation path is deterministic for the same stored paper.
  </acceptance_criteria>
  <done>The repo has a real, typed enrichment artifact and a runnable generator.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist and expose enrichment through CLI, export, serve, and MCP</name>
  <files>packages/cli/src/store.ts, packages/cli/src/index.ts, packages/cli/src/export.ts, packages/cli/src/server.ts, packages/mcp/src/server.ts, packages/cli/test/, packages/mcp/test/server.test.ts</files>
  <action>Add `paperparser enrich` as an opt-in second-pass command on stored papers. Persist `enrichment.json` beside the canonical bundle, copy it during export when present, expose it from the HTTP API, and make the MCP enrichment resource return the real sidecar rather than `index.json`.</action>
  <verify>
    <automated>npx vitest run packages/cli/test/enrich-command.test.ts packages/cli/test/export-command.test.ts packages/cli/test/serve-app.test.ts packages/mcp/test/server.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Running `paperparser enrich` creates `enrichment.json` without modifying the canonical bundle files.
    - Export includes the enrichment sidecar when present.
    - HTTP API and MCP resource reads can retrieve the enrichment sidecar.
  </acceptance_criteria>
  <done>All delivery surfaces can access the optional sidecar.</done>
</task>

</tasks>

<verification>
<automated>npx vitest run packages/core/test/enrichment-service.test.ts packages/cli/test/enrich-command.test.ts packages/cli/test/export-command.test.ts packages/cli/test/serve-app.test.ts packages/mcp/test/server.test.ts</automated>
</verification>

<success_criteria>
Plan 04-01 is complete when the repo has a validated, separately stored enrichment artifact and a runnable opt-in generation path exposed across CLI, export, serve, and MCP.
</success_criteria>

<output>
After completion, create `.planning/phases/04-optional-agent-enrichment-review/04-01-SUMMARY.md`
</output>
