---
phase: 17-server-deployment-boundary-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/server.ts
  - packages/cli/src/index.ts
  - packages/cli/test/serve-app.test.ts
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - OPS-01
must_haves:
  truths:
    - Shared deployment must not allow arbitrary remote JSON `inputPath` analysis by default.
    - Request and upload handling must fail within explicit bounds instead of buffering unbounded bodies in memory.
    - Health/readiness and structured logs must exist at the app boundary before Cloud Run packaging claims are made.
  artifacts:
    - path: packages/cli/src/server.ts
      provides: explicit runtime mode, bounded request handling, health/readiness routes, and structured request/error logging
    - path: packages/cli/src/index.ts
      provides: serve entrypoint wiring for the new runtime mode and limits surface
    - path: packages/cli/test/serve-app.test.ts
      provides: request-boundary proof for local-vs-deployed behavior, request limits, and health/readiness routes
  key_links:
    - from: packages/cli/src/server.ts
      to: packages/cli/test/serve-app.test.ts
      via: request/response regressions
      pattern: deployed mode rejects JSON `inputPath`, bounded bodies fail explicitly, and health/readiness routes stay green
    - from: packages/cli/src/index.ts
      to: packages/cli/src/server.ts
      via: runtime option wiring
      pattern: serve command can select the safe deployed boundary without changing the rest of the app entrypoint
---

<objective>
Finish Phase 17 by hardening the current API boundary so it can safely underlie a later Cloud Run deployment.

Purpose: eliminate the clearest localhost-only trust assumptions and add the minimum operability surface needed for shared deployment.
Output: explicit deployed-mode behavior, bounded request/upload handling, `/healthz`, `/readyz`, structured logs, and automated request-boundary proof.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-server-deployment-boundary-hardening/17-CONTEXT.md
@.planning/phases/17-server-deployment-boundary-hardening/17-RESEARCH.md
@docs/deployment_readiness.md
@packages/cli/src/server.ts
@packages/cli/src/index.ts
@packages/cli/test/serve-app.test.ts

Locked decisions to honor in this phase:
- Introduce a deploy-safe runtime boundary without dragging Cloud Run packaging or auth policy into this phase.
- Keep local `inputPath` ergonomics only if deployed mode rejects them explicitly.
- Prioritize explicit request/upload size limits and bounded failure behavior over a full streaming redesign.
- Add app-level health/readiness routes and structured logs now, not in a later packaging phase.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the deploy-safe server contract in failing request-boundary tests</name>
  <files>packages/cli/test/serve-app.test.ts</files>
  <action>Add request-boundary regressions for deployed-mode JSON `inputPath` rejection, oversized request/upload failure behavior, and `/healthz` / `/readyz` routes before changing production code.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/serve-app.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - At least one new regression fails before server changes exist.
    - Tests distinguish local-mode behavior from deployed-mode behavior.
    - Tests lock explicit status codes and error messages for bounded failure states.
  </acceptance_criteria>
  <done>The desired Phase 17 boundary is executable before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deployed-mode request hardening and app health surfaces</name>
  <files>packages/cli/src/server.ts, packages/cli/src/index.ts</files>
  <action>Add runtime-mode-aware analyze behavior, explicit request/upload limits, `/healthz` and `/readyz`, and structured request/error logging, then wire the serve entrypoint to the new runtime options.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/serve-app.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Deployed mode rejects JSON `inputPath` analysis explicitly.
    - Oversized request bodies fail before unbounded buffering completes.
    - Health/readiness routes return deterministic success when the app is usable.
    - Structured log events exist for normal requests and request-handler failures.
  </acceptance_criteria>
  <done>The server boundary is explicitly safer and more diagnosable without relying on later Cloud Run phases.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Re-prove the request boundary and typing after the hardening</name>
  <files>packages/cli/test/serve-app.test.ts</files>
  <action>Re-run the server boundary tests and repo typecheck to prove the safer runtime contract without regressing the local API entrypoint.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/serve-app.test.ts</automated>
    <automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The request boundary passes in both the existing local flow and the new deployed-mode assertions.
    - The CLI package and cross-workspace typecheck stay green.
  </acceptance_criteria>
  <done>The Phase 17 server hardening is re-proven at the request boundary and the repo typing boundary.</done>
</task>

</tasks>

<verification>
<automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/serve-app.test.ts</automated>
<automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
</verification>

<success_criteria>
Plan 17-01 is complete when deployed mode no longer exposes arbitrary remote JSON `inputPath` analysis, request/upload limits fail explicitly, `/healthz` and `/readyz` exist, structured server logs are emitted, and automated request-boundary tests plus typecheck prove the new contract.
</success_criteria>

<output>
After completion, create `.planning/phases/17-server-deployment-boundary-hardening/17-01-SUMMARY.md`
</output>
