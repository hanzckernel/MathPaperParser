---
phase: 11-dashboard-math-rendering-repair
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/package.json
  - package-lock.json
  - packages/web/src/components/dashboard-pages.tsx
  - packages/web/src/components/proof-graph-page.tsx
  - packages/web/src/lib/math-render.tsx
  - packages/web/test/math-render.test.ts
  - packages/web/test/proof-graph-render.test.ts
autonomous: true
requirements:
  - MATH-01
  - MATH-02
  - MATH-03
must_haves:
  truths:
    - Statement-bearing reading surfaces must render math with MathJax instead of showing raw TeX-style strings where rendering is expected.
    - Render-time normalization must repair common hard-line-break and package-dependent fragments without changing the canonical bundle text contract.
    - Unsupported fragments must degrade to an explicit inline raw-source fallback without breaking the surrounding page.
  artifacts:
    - path: packages/web/src/lib/math-render.tsx
      provides: Shared normalization rules, MathJax loading/render lifecycle, and inline fallback behavior
    - path: packages/web/src/components/dashboard-pages.tsx
      provides: Theorem Explorer statement rendering through the shared math wrapper
    - path: packages/web/src/components/proof-graph-page.tsx
      provides: Proof Graph detail/sidebar statement rendering through the shared math wrapper
    - path: packages/web/test/math-render.test.ts
      provides: Direct regression proof for normalization and fallback decisions, including synthetic hard-line-break damage
    - path: packages/web/test/proof-graph-render.test.ts
      provides: Fixture-backed proof that reading surfaces mount the math-rendering wrapper instead of plain raw statement text
  key_links:
    - from: packages/web/src/lib/math-render.tsx
      to: packages/web/src/components/dashboard-pages.tsx
      via: shared statement renderer used in the current theorem explorer
      pattern: render normalized statement text with local fallback
    - from: packages/web/src/lib/math-render.tsx
      to: packages/web/src/components/proof-graph-page.tsx
      via: shared statement renderer used in the proof-graph detail view
      pattern: identical MathJax/fallback behavior across statement surfaces
    - from: packages/web/test/math-render.test.ts
      to: packages/web/src/lib/math-render.tsx
      via: direct normalization and fallback regression coverage
      pattern: line-broken or package-dependent fragments normalize or fall back explicitly
---

<objective>
Finish Phase 11 by repairing the current dashboard’s statement-reading surfaces with MathJax plus a project-owned normalization pass.

Purpose: make theorem and equation text readable in the existing dashboard without changing the bundle contract and without spilling into broader runtime/bootstrap work.
Output: a shared MathJax renderer with aggressive fragment normalization, inline raw fallback, and fixture-backed regression coverage on the current explorer/detail statement surfaces.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-dashboard-math-rendering-repair/11-CONTEXT.md
@docs/dashboard_spec.md
@packages/web/package.json
@packages/web/src/components/dashboard-pages.tsx
@packages/web/src/components/proof-graph-page.tsx
@packages/web/src/lib/dashboard-model.ts
@packages/web/test/proof-graph-render.test.ts

Locked decisions to honor in this phase:
- Apply MathJax to the medium-scope statement-bearing reading surfaces only.
- Use an aggressive project-owned normalization pass before rendering.
- Keep normalization at render time; do not mutate the canonical bundle text.
- Fall back inline to a clearly marked raw-source block when safe rendering still fails.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the math-rendering contract in failing tests</name>
  <files>packages/web/test/math-render.test.ts, packages/web/test/proof-graph-render.test.ts</files>
  <action>Add direct regressions for normalization and fallback behavior, plus a render-level regression proving the current statement surfaces use the shared math-rendering wrapper instead of plain text blocks.</action>
  <verify>
    <automated>npm test -- packages/web/test/math-render.test.ts</automated>
    <automated>npm test -- packages/web/test/proof-graph-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - At least one synthetic fragment with hard line breaks or unsupported wrappers fails before the implementation exists.
    - The proof-graph/theorem-explorer render test demonstrates that statement surfaces mount the new wrapper contract.
  </acceptance_criteria>
  <done>The Phase 11 acceptance bar exists as executable regressions before production changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the shared MathJax normalization and render helper</name>
  <files>packages/web/package.json, package-lock.json, packages/web/src/lib/math-render.tsx</files>
  <action>Use the official MathJax package, implement render-time normalization for common broken fragments and package-dependent commands, and expose a shared statement component that typesets safely or falls back inline.</action>
  <verify>
    <automated>npm test -- packages/web/test/math-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The implementation uses project-owned normalization rather than broad `amsmath` or `amsthm` browser add-ons.
    - Unsupported commands such as simple `\\ref`, `\\eqref`, or theorem-style wrappers are rewritten or routed to fallback predictably.
    - Failures remain local to the affected statement block.
  </acceptance_criteria>
  <done>A reusable MathJax rendering boundary exists and satisfies the direct regression tests.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire the reading surfaces through the shared math renderer</name>
  <files>packages/web/src/components/dashboard-pages.tsx, packages/web/src/components/proof-graph-page.tsx, packages/web/test/proof-graph-render.test.ts</files>
  <action>Replace the current plain-text statement blocks in the Theorem Explorer and Proof Graph detail panel with the shared MathJax wrapper while leaving unrelated prose/metadata fields alone.</action>
  <verify>
    <automated>npm test -- packages/web/test/proof-graph-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both current statement-reading surfaces use the same normalization/render/fallback behavior.
    - Non-statement surfaces remain plain text in this phase.
  </acceptance_criteria>
  <done>The approved medium-scope reading surfaces are repaired without broadening Phase 11.</done>
</task>

</tasks>

<verification>
<automated>npm test -- packages/web/test/math-render.test.ts</automated>
<automated>npm test -- packages/web/test/proof-graph-render.test.ts</automated>
<automated>npm run typecheck</automated>
</verification>

<success_criteria>
Plan 11-01 is complete when the current dashboard’s statement-reading surfaces render through MathJax with aggressive render-time normalization, unsupported fragments degrade to an explicit inline raw fallback, and automated tests prove both the helper behavior and the wired statement surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/11-dashboard-math-rendering-repair/11-01-SUMMARY.md`
</output>
