---
phase: 13-export-acceptance-operator-guidance
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - README.md
  - docs/user_guide.md
  - docs/deployment_readiness.md
  - packages/web/test/operator-guidance-docs.test.ts
autonomous: true
requirements:
  - REL-01
  - REL-02
must_haves:
  truths:
    - The repo must publish a named acceptance-proof command for the completed Phase 10-12 regression bundle.
    - The operator docs must describe the shipped static export contract accurately, including `enrichment.json`, bundled MathJax rendering, and the HTTP-only serving requirement for static exports.
    - The docs must keep the current stack explicitly local-first and non-production.
  artifacts:
    - path: package.json
      provides: Reproducible acceptance-proof command for the v1.2 regression bundle
    - path: README.md
      provides: Top-level milestone proof and local export/serve guidance
    - path: docs/user_guide.md
      provides: Detailed local export workflow, static serving requirement, and MathJax/export contract details
    - path: docs/deployment_readiness.md
      provides: Explicit non-production positioning aligned with the accepted local workflow
    - path: packages/web/test/operator-guidance-docs.test.ts
      provides: Regression proof that the acceptance command and critical operator docs remain present
  key_links:
    - from: package.json
      to: README.md
      via: named acceptance-proof command
      pattern: top-level docs point users to one reproducible proof command
    - from: README.md
      to: docs/user_guide.md
      via: local export and serve workflow
      pattern: quickstart stays concise while the user guide carries the detailed operator contract
    - from: packages/web/test/operator-guidance-docs.test.ts
      to: package.json and docs/user_guide.md
      via: documentation contract regression
      pattern: acceptance command and critical export/runtime instructions remain checked in
---

<objective>
Finish the milestone by making the accepted local export workflow explicit, reproducible, and aligned with the completed hardening work.

Purpose: give users one proof command for the milestone and docs they can follow without guessing hidden prerequisites.
Output: named acceptance-proof command, aligned README/user guide/deployment guidance, and regression coverage that keeps the operator contract from drifting.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-export-acceptance-operator-guidance/13-CONTEXT.md
@README.md
@package.json
@docs/user_guide.md
@docs/deployment_readiness.md
@docs/dashboard_spec.md
@packages/web/test/operator-guidance-docs.test.ts

Locked expectations to honor in this phase:
- Publish a focused acceptance-proof command for the hardened export/dashboard workflow.
- Document `data/enrichment.json`, bundled MathJax rendering behavior, and the HTTP-only requirement for static exports.
- Keep the guidance explicitly local-first and non-production.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the missing acceptance command and docs contract in a failing regression</name>
  <files>packages/web/test/operator-guidance-docs.test.ts</files>
  <action>Add a docs-contract regression that requires a dedicated milestone proof command plus explicit README/user-guide coverage for the static export/runtime/math story.</action>
  <verify>
    <automated>npm test -- packages/web/test/operator-guidance-docs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The regression fails before the new acceptance command and docs updates exist.
    - The test checks for `data/enrichment.json`, HTTP serving guidance, and MathJax mention in the operator docs.
  </acceptance_criteria>
  <done>The missing acceptance/docs contract is executable and red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish the milestone proof command and align the top-level docs</name>
  <files>package.json, README.md, docs/user_guide.md, docs/deployment_readiness.md</files>
  <action>Add a named v1.2 acceptance script and update the top-level/operator docs so the hardened export workflow is accurate, explicit, and reproducible.</action>
  <verify>
    <automated>npm test -- packages/web/test/operator-guidance-docs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Users can copy a single acceptance command from the repo.
    - README and user guide mention `enrichment.json`, MathJax rendering, and the `file://`/HTTP requirement clearly.
    - The docs do not imply internet-facing production readiness.
  </acceptance_criteria>
  <done>The local export/serve workflow is documented accurately and backed by a named proof command.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Re-run the named acceptance proof and workspace typecheck</name>
  <files>package.json</files>
  <action>Use the new acceptance script as the milestone proof point, then run typecheck so the milestone closes on fresh evidence.</action>
  <verify>
    <automated>npm run test:acceptance:v1.2</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The acceptance script covers export, math rendering, shell bootstrap, and runtime guard behavior.
    - The documented proof commands run cleanly from the repo root.
  </acceptance_criteria>
  <done>The milestone proof is reproducible from the checked-in repo instructions.</done>
</task>

</tasks>

<verification>
<automated>npm test -- packages/web/test/operator-guidance-docs.test.ts</automated>
<automated>npm run test:acceptance:v1.2</automated>
<automated>npm run typecheck</automated>
</verification>

<success_criteria>
Plan 13-01 is complete when the repo publishes a named acceptance-proof command, the README and user guide accurately describe the hardened export/runtime/math behavior, and the documented proof commands run cleanly from the repo root.
</success_criteria>

<output>
After completion, create `.planning/phases/13-export-acceptance-operator-guidance/13-01-SUMMARY.md`
</output>
