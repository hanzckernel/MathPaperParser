---
phase: 18-cloud-run-packaging-topology
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile
  - .dockerignore
  - packages/cli/src/server.ts
  - packages/cli/src/index.ts
  - packages/cli/test/serve-app.test.ts
  - packages/web/src/lib/data-source.ts
  - packages/web/src/App.tsx
  - packages/web/test/bundle-data.test.ts
  - README.md
  - docs/deployment_readiness.md
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
must_haves:
  truths:
    - The repo must ship a Cloud Run deployment artifact that builds and starts the supported combined service from repo-defined inputs.
    - The deployed dashboard must reach the API through the same origin without requiring a manual split-origin URL dance.
    - The packaging/topology contract must stay explicit and leave auth/ingress enforcement for the next security phase.
  artifacts:
    - path: Dockerfile
      provides: supported repo-defined Cloud Run deployment artifact
    - path: packages/cli/src/server.ts
      provides: combined same-origin web/API serving contract
    - path: packages/web/src/lib/data-source.ts
      provides: deployed runtime-config source resolution to same-origin API mode
    - path: packages/cli/test/serve-app.test.ts
      provides: request-boundary proof for packaged dashboard serving
    - path: packages/web/test/bundle-data.test.ts
      provides: source-resolution proof for deployed same-origin browser behavior
  key_links:
    - from: packages/cli/src/server.ts
      to: packages/web/src/lib/data-source.ts
      via: injected runtime config
      pattern: combined service can serve the dashboard shell and the browser will bind to the same-origin API by default
    - from: Dockerfile
      to: packages/cli/src/index.ts
      via: runtime entrypoint
      pattern: Cloud Run image starts the existing CLI server in deployed mode with the packaged web bundle
---

<objective>
Finish Phase 18 by shipping the first supported Cloud Run artifact and the first explicit combined same-origin deployment topology.

Purpose: replace the repo's missing packaging story and implicit browser/API topology with a repo-owned container plus a combined web/API runtime contract.
Output: root Dockerfile, packaged dashboard serving from the CLI server, explicit deployed runtime config for same-origin API mode, and automated proof.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-cloud-run-packaging-topology/18-CONTEXT.md
@.planning/phases/18-cloud-run-packaging-topology/18-RESEARCH.md
@docs/deployment_readiness.md
@README.md
@packages/cli/src/server.ts
@packages/cli/src/index.ts
@packages/cli/test/serve-app.test.ts
@packages/web/src/lib/data-source.ts
@packages/web/src/App.tsx
@packages/web/test/bundle-data.test.ts
@packages/web/vite.config.ts

Locked decisions to honor in this phase:
- Use one repo-owned Cloud Run container artifact, not an ad hoc deploy recipe.
- Support one combined same-origin web/API deployment shape.
- Keep static export behavior intact; the deployed combined-service path is an additional explicit runtime contract.
- Do not fold auth/authz or ingress lock-down into this phase.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the combined-service topology in failing tests</name>
  <files>packages/cli/test/serve-app.test.ts, packages/web/test/bundle-data.test.ts</files>
  <action>Add regressions for serving a configured dashboard shell/assets from the CLI server and for runtime-config-driven same-origin API mode in the web app before changing production code.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/serve-app.test.ts packages/web/test/bundle-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - At least one new request/data-source regression fails before implementation.
    - Tests prove the deployed dashboard can default to the same-origin API contract without breaking static export defaults.
    - Tests pin the HTTP response shape for the combined-service shell/assets path.
  </acceptance_criteria>
  <done>The supported topology is executable before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement packaged dashboard serving and deployed runtime config</name>
  <files>packages/cli/src/server.ts, packages/cli/src/index.ts, packages/web/src/lib/data-source.ts, packages/web/src/App.tsx</files>
  <action>Extend the CLI server to serve a configured built dashboard, inject a deployed runtime config into the served shell, and teach the web app to prefer same-origin API mode from that runtime config while preserving current static behavior.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/serve-app.test.ts packages/web/test/bundle-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The CLI server can serve `index.html` and bundled assets from a configured web dist path.
    - The deployed dashboard defaults to same-origin API mode without requiring manual query params.
    - Static export mode remains the default when no deployed runtime config exists.
  </acceptance_criteria>
  <done>The combined service is a first-class runtime contract, not an undocumented proxy assumption.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Ship the Cloud Run artifact and align the docs</name>
  <files>Dockerfile, .dockerignore, README.md, docs/deployment_readiness.md</files>
  <action>Add the supported Cloud Run deployment artifact and update the deployment docs so the repo accurately describes the new packaging/topology baseline and remaining blockers.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/serve-app.test.ts packages/web/test/bundle-data.test.ts</automated>
    <automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The repo root contains the supported container artifact.
    - Docs no longer claim there is no supported packaging artifact or no supported combined web/API topology.
    - Remaining deployment blockers stay explicit instead of being hand-waved away.
  </acceptance_criteria>
  <done>The repo advertises the supported Cloud Run packaging/topology shape accurately.</done>
</task>

</tasks>

<verification>
<automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/serve-app.test.ts packages/web/test/bundle-data.test.ts</automated>
<automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
</verification>

<success_criteria>
Plan 18-01 is complete when the repo ships a Cloud Run container artifact, the CLI server can serve the built dashboard as a same-origin companion to the API, the web app can default into that deployed same-origin API mode without breaking static export mode, and automated server/data-source tests plus typecheck prove the contract.
</success_criteria>

<output>
After completion, create `.planning/phases/18-cloud-run-packaging-topology/18-01-SUMMARY.md`
</output>
