---
phase: 14-residual-tex-parser-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/ingestion/parsers/latex-parser.ts
  - packages/core/src/types/pipeline.ts
  - packages/core/test/fixtures/latex/gold-paper-regressions/duplicate-labels.tex
  - packages/core/test/ingestion-pipeline.test.ts
  - packages/core/test/gold-paper-ingestion.test.ts
  - packages/cli/test/gold-paper-acceptance.test.ts
autonomous: true
requirements:
  - HARD-06
  - HARD-07
  - HARD-08
must_haves:
  truths:
    - Residual parser hardening must lower the accepted-corpus unresolved-reference budget without adding figure-schema work.
    - One extracted target may need multiple resolvable labels, but duplicate labels across distinct targets must stay explicit.
    - Bounded `\cref` / `\Cref` support must resolve only to already-known deterministic targets and must not imply broad cleveref emulation.
  artifacts:
    - path: packages/core/src/ingestion/parsers/latex-parser.ts
      provides: Alias-aware label registration, duplicate-label handling, multiline heading support, and bounded cleveref resolution
    - path: packages/core/src/types/pipeline.ts
      provides: Explicit warning shape support for duplicate-label diagnostics if Phase 14 adds a new warning code
    - path: packages/core/test/fixtures/latex/gold-paper-regressions/duplicate-labels.tex
      provides: Focused regression coverage for first-definition-wins duplicate-label behavior
    - path: packages/core/test/ingestion-pipeline.test.ts
      provides: Fixture-level proof for multiline headings, secondary labels, cleveref resolution, and duplicate-label warnings
    - path: packages/core/test/gold-paper-ingestion.test.ts
      provides: Accepted-corpus unresolved-budget proof after the parser hardening
    - path: packages/cli/test/gold-paper-acceptance.test.ts
      provides: End-to-end stored diagnostics proof for the lowered real-corpus residual budget
  key_links:
    - from: packages/core/src/ingestion/parsers/latex-parser.ts
      to: packages/core/test/ingestion-pipeline.test.ts
      via: fixture-backed residual parser regressions
      pattern: multiline headings, multiple labels, and bounded `\cref` support resolve deterministically
    - from: packages/core/src/ingestion/parsers/latex-parser.ts
      to: packages/core/test/gold-paper-ingestion.test.ts
      via: accepted-corpus warning-budget lock
      pattern: `long_nalini` unresolved references drop below the `v1.2` baseline while explicit residuals remain
    - from: packages/core/src/ingestion/parsers/latex-parser.ts
      to: packages/cli/test/gold-paper-acceptance.test.ts
      via: persisted diagnostics sidecar
      pattern: CLI acceptance sees the same lowered unresolved budget as the core parser tests
---

<objective>
Finish Phase 14 by tightening deterministic label extraction and reference resolution on the accepted corpus without expanding the canonical schema.

Purpose: reduce the residual parse failures still affecting mathematical reading and lookup, while keeping unsupported figure targets and other out-of-scope constructs explicit.
Output: multiline heading labels, multi-label targets, bounded `\cref` / `\Cref` support, explicit duplicate-label diagnostics, and accepted-corpus warning-budget proof.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-residual-tex-parser-hardening/14-CONTEXT.md
@.planning/phases/14-residual-tex-parser-hardening/14-RESEARCH.md
@packages/core/src/types/pipeline.ts
@packages/core/src/ingestion/parsers/latex-parser.ts
@packages/core/test/ingestion-pipeline.test.ts
@packages/core/test/gold-paper-ingestion.test.ts
@packages/cli/test/gold-paper-acceptance.test.ts

Locked decisions to honor in this phase:
- Figure references stay explicit; do not add figure-target or schema work here.
- Bounded `\cref` / `\Cref` support is allowed only when the parser already knows the deterministic target.
- Duplicate labels resolve to the first definition and emit an explicit warning.
- Stay accepted-corpus first, with room for one adjacent deterministic pattern class if it falls out naturally.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the residual parser seams in failing regressions</name>
  <files>packages/core/test/fixtures/latex/gold-paper-regressions/duplicate-labels.tex, packages/core/test/ingestion-pipeline.test.ts</files>
  <action>Add focused parser regressions covering multiline subsection labels, multiple labels on one target, duplicate-label warning semantics, and bounded `\cref` / `\Cref` resolution to already-known nodes.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/core/test/ingestion-pipeline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - At least one new regression fails before the parser changes exist.
    - The new fixture proves first-definition-wins duplicate-label handling explicitly.
    - The tests distinguish resolvable bounded `\cref` cases from still-explicit unsupported residuals.
  </acceptance_criteria>
  <done>The approved Phase 14 parser contract exists as executable regressions before production changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden label registration and bounded cleveref resolution in the parser</name>
  <files>packages/core/src/ingestion/parsers/latex-parser.ts, packages/core/src/types/pipeline.ts</files>
  <action>Refactor the parser so supported headings and statement blocks can register multiple label aliases to one node, preserve first-definition-wins duplicate-label warnings, and resolve bounded `\cref` / `\Cref` references only when the target is already known deterministically.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/core/test/ingestion-pipeline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Multiline subsection labels such as `s:coord` resolve.
    - Secondary labels such as `lem:df` and the residual `e:...` aliases resolve to the same extracted node as their primary label.
    - Duplicate labels emit an explicit warning and keep the first target.
    - Figure-target `\cref` cases remain explicit rather than silently mis-resolving.
  </acceptance_criteria>
  <done>The deterministic parser resolves the measured non-figure residual classes without broad schema churn.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Re-lock the accepted-corpus and CLI diagnostics budgets</name>
  <files>packages/core/test/gold-paper-ingestion.test.ts, packages/cli/test/gold-paper-acceptance.test.ts</files>
  <action>Update the real-corpus and end-to-end acceptance tests to prove the lowered `long_nalini` unresolved-reference budget while preserving explicit residual diagnostics and bundle validity.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/core/test/gold-paper-ingestion.test.ts packages/cli/test/gold-paper-acceptance.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The accepted corpus stays schema-valid and consistency-valid.
    - `long_nalini` unresolved references fall below the shipped `v1.2` baseline of `22`.
    - Remaining explicit residuals stay inspectable through diagnostics rather than disappearing silently.
  </acceptance_criteria>
  <done>The real-corpus warning budget and stored diagnostics contract are re-proven after the parser hardening.</done>
</task>

</tasks>

<verification>
<automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/core/test/ingestion-pipeline.test.ts</automated>
<automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/core/test/gold-paper-ingestion.test.ts packages/cli/test/gold-paper-acceptance.test.ts</automated>
<automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
</verification>

<success_criteria>
Plan 14-01 is complete when the deterministic parser resolves the measured non-figure residual classes on the accepted corpus, duplicate labels stay explicit and deterministic, bounded `\cref` / `\Cref` support is in place for known targets, and automated tests prove the lower warning budget without canonical-bundle regressions.
</success_criteria>

<output>
After completion, create `.planning/phases/14-residual-tex-parser-hardening/14-01-SUMMARY.md`
</output>
