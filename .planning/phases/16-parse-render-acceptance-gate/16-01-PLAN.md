---
phase: 16-parse-render-acceptance-gate
plan: "01"
type: execute
wave: 1
depends_on:
  - 14-01
  - 15-01
files_modified:
  - package.json
  - README.md
  - docs/user_guide.md
  - packages/web/test/operator-guidance-docs.test.ts
autonomous: true
requirements:
  - ACC-04
  - ACC-05
must_haves:
  truths:
    - Phase 16 must publish one reproducible `v1.3` acceptance command for the upgraded parse/render workflow.
    - The acceptance bundle must cover both accepted-corpus workflow evidence and targeted parser/render regression fixtures.
    - The current proof command must be discoverable from the repo root docs rather than only from planning artifacts.
  artifacts:
    - path: package.json
      provides: Named `v1.3` acceptance proof command
    - path: README.md
      provides: Top-level discovery path for the current milestone proof
    - path: docs/user_guide.md
      provides: Operator-facing acceptance workflow for the upgraded parse/render milestone
    - path: packages/web/test/operator-guidance-docs.test.ts
      provides: Regression proof that the latest milestone acceptance command remains published and documented
  key_links:
    - from: package.json
      to: README.md
      via: named milestone proof command
      pattern: the repo root tells users exactly which command proves the current parse/render milestone
    - from: README.md
      to: docs/user_guide.md
      via: concise-to-detailed workflow guidance
      pattern: the top-level docs point to the detailed operator path without drifting from the actual command
    - from: packages/web/test/operator-guidance-docs.test.ts
      to: package.json and docs/user_guide.md
      via: docs-contract regression
      pattern: the acceptance command and parse/render workflow remain checked in and discoverable
---

<objective>
Finish `v1.3` by publishing and re-running one reproducible parse/render acceptance proof.

Purpose: make the upgraded parser and renderer verifiable from the repo root with a named command instead of scattered manual test invocations.
Output: `test:acceptance:v1.3`, aligned README/user guide references, a docs-contract regression, and fresh verification evidence.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-parse-render-acceptance-gate/16-RESEARCH.md
@package.json
@README.md
@docs/user_guide.md
@packages/web/test/operator-guidance-docs.test.ts
@packages/core/test/ingestion-pipeline.test.ts
@packages/core/test/gold-paper-ingestion.test.ts
@packages/cli/test/gold-paper-acceptance.test.ts
@packages/web/test/math-render.test.ts
@packages/web/test/proof-graph-render.test.ts

Locked expectations to honor in this phase:
- Keep the acceptance bundle focused on parse/render hardening rather than re-scoping to all `v1.2` export/runtime work.
- Use a named repo-level proof command instead of a new custom harness script.
- Cover both real-corpus workflow evidence and targeted regression fixtures in the same named proof.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the missing `v1.3` proof contract in a failing docs regression</name>
  <files>packages/web/test/operator-guidance-docs.test.ts</files>
  <action>Extend the existing docs-contract test so it requires a dedicated `test:acceptance:v1.3` command and matching README/user-guide references for the current parse/render milestone.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/web/test/operator-guidance-docs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The regression fails before the new command and docs updates exist.
    - The test proves the current milestone command is published and documented, not only present in planning files.
  </acceptance_criteria>
  <done>The missing Phase 16 proof contract is executable and red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish the `v1.3` parse/render proof command and align the docs</name>
  <files>package.json, README.md, docs/user_guide.md</files>
  <action>Add `test:acceptance:v1.3` as a focused parser/render acceptance bundle and update the top-level/operator docs so users can discover and rerun the current milestone proof cleanly.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/web/test/operator-guidance-docs.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The new script includes accepted-corpus parser validation, CLI workflow proof, and targeted parser/render regressions.
    - README points to the current milestone proof command.
    - The user guide documents the parse/render acceptance workflow without implying that `v1.2` export/runtime proof disappeared.
  </acceptance_criteria>
  <done>The repo exposes one reproducible proof command for the completed parse/render milestone.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Re-run the named milestone proof and workspace typecheck</name>
  <files>package.json</files>
  <action>Use the new acceptance script as the milestone proof point, then rerun workspace typecheck so closeout is backed by fresh evidence.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm run test:acceptance:v1.3</automated>
    <automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The named proof command runs successfully from the repo root.
    - The command covers parser hardening, render hardening, and the accepted local workflow together.
    - Workspace typecheck remains green after the acceptance-command/docs edits.
  </acceptance_criteria>
  <done>The milestone proof is reproducible from checked-in repo instructions and backed by fresh verification output.</done>
</task>

</tasks>

<verification>
<automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/web/test/operator-guidance-docs.test.ts</automated>
<automated>PATH=/opt/homebrew/bin:$PATH npm run test:acceptance:v1.3</automated>
<automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
</verification>

<success_criteria>
Plan 16-01 is complete when the repo publishes `test:acceptance:v1.3`, the current docs point to it clearly, and the command plus workspace typecheck pass as the final `v1.3` milestone evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/16-parse-render-acceptance-gate/16-01-SUMMARY.md`
</output>
