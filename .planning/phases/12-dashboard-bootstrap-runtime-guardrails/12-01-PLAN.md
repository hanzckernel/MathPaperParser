---
phase: 12-dashboard-bootstrap-runtime-guardrails
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/App.tsx
  - packages/web/src/components/runtime-blocker.tsx
  - packages/web/src/lib/bootstrap.ts
  - packages/web/src/lib/runtime-environment.ts
  - packages/web/test/runtime-blocker-render.test.ts
  - packages/web/test/bootstrap.test.ts
  - packages/web/test/runtime-environment.test.ts
  - packages/web/test/exported-dashboard-shell.test.ts
autonomous: true
requirements:
  - EXPORT-03
  - DASH-01
  - DASH-02
  - DASH-03
must_haves:
  truths:
    - Unsupported static `file://` usage must fail explicitly with a full-page blocker rather than a generic in-shell load error.
    - The dashboard must enforce `#root` as the only valid mount target.
    - API-backed mode must remain usable even from `file://`.
  artifacts:
    - path: packages/web/src/components/runtime-blocker.tsx
      provides: Dedicated full-page unsupported-runtime blocker view with actionable local-server command
    - path: packages/web/src/App.tsx
      provides: Top-level runtime blocker routing before the normal shell renders
    - path: packages/web/src/lib/bootstrap.ts
      provides: Strict `#root` bootstrap contract
    - path: packages/web/src/lib/runtime-environment.ts
      provides: Static-only `file://` blocker logic that leaves API mode alone
    - path: packages/web/test/runtime-blocker-render.test.ts
      provides: Direct regression proof for the full-page blocker content and command
    - path: packages/web/test/bootstrap.test.ts
      provides: Strict mount-target proof
    - path: packages/web/test/runtime-environment.test.ts
      provides: Static-vs-API runtime guard proof
    - path: packages/web/test/exported-dashboard-shell.test.ts
      provides: Exported shell `#root` contract proof
  key_links:
    - from: packages/web/src/lib/runtime-environment.ts
      to: packages/web/src/App.tsx
      via: top-level runtime classification before bundle loading
      pattern: static `file://` yields a blocker page, API mode does not
    - from: packages/web/src/lib/bootstrap.ts
      to: packages/web/src/main.tsx
      via: strict mount-target resolution
      pattern: `#root` missing is a bootstrap error, not a fallback path
    - from: packages/web/test/exported-dashboard-shell.test.ts
      to: packages/web/index.html
      via: exported shell mount-target contract
      pattern: exported HTML contains `#root` and not `#app`
---

<objective>
Finish Phase 12 by turning the current bootstrap/runtime safeguards into explicit product behavior at the app boundary.

Purpose: make supported exports mount reliably over HTTP while making unsupported static `file://` usage fail clearly, without blocking valid API-backed operation.
Output: strict `#root` bootstrap, top-level static runtime blocker, preserved API mode, and automated regressions for shell/runtime behavior.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-dashboard-bootstrap-runtime-guardrails/12-CONTEXT.md
@docs/dashboard_spec.md
@packages/web/index.html
@packages/web/src/main.tsx
@packages/web/src/App.tsx
@packages/web/src/lib/bootstrap.ts
@packages/web/src/lib/runtime-environment.ts
@packages/web/test/bootstrap.test.ts
@packages/web/test/runtime-environment.test.ts
@packages/web/test/exported-dashboard-shell.test.ts

Locked decisions to honor in this phase:
- Static `file://` usage gets a full-page blocker with an exact local-server command.
- Missing `#root` is an explicit bootstrap error, not a fallback case.
- API-backed mode remains allowed over `file://`.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the missing full-page blocker behavior in a failing regression</name>
  <files>packages/web/test/runtime-blocker-render.test.ts</files>
  <action>Add a render-level regression for the unsupported static runtime blocker so the current generic in-shell error treatment is not considered sufficient.</action>
  <verify>
    <automated>npm test -- packages/web/test/runtime-blocker-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The new regression fails before the blocker view exists.
    - The test checks for a full-page blocker heading and the exact local-server command.
  </acceptance_criteria>
  <done>The missing top-level blocker contract is executable and red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route unsupported static runtime through a dedicated blocker view</name>
  <files>packages/web/src/components/runtime-blocker.tsx, packages/web/src/App.tsx</files>
  <action>Add a dedicated full-page blocker component and make the app return it before rendering the normal shell when static exports are opened from `file://`.</action>
  <verify>
    <automated>npm test -- packages/web/test/runtime-blocker-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The blocker view is explicit, actionable, and visually separate from generic load errors.
    - The normal dashboard shell and controls do not render behind the unsupported static blocker.
  </acceptance_criteria>
  <done>Unsupported static runtime conditions are expressed as a deliberate product state.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Re-verify the strict bootstrap and runtime boundaries end to end</name>
  <files>packages/web/test/bootstrap.test.ts, packages/web/test/runtime-environment.test.ts, packages/web/test/exported-dashboard-shell.test.ts</files>
  <action>Keep the strict `#root` and API/static distinction green while the app-level blocker view is introduced.</action>
  <verify>
    <automated>npm test -- packages/web/test/bootstrap.test.ts packages/web/test/runtime-environment.test.ts packages/web/test/exported-dashboard-shell.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `#root` remains the only accepted mount target.
    - Static `file://` remains blocked.
    - API mode over `file://` remains allowed.
  </acceptance_criteria>
  <done>The app-level blocker does not regress the lower-level runtime guarantees already present in the worktree.</done>
</task>

</tasks>

<verification>
<automated>npm test -- packages/web/test/runtime-blocker-render.test.ts</automated>
<automated>npm test -- packages/web/test/bootstrap.test.ts packages/web/test/runtime-environment.test.ts packages/web/test/exported-dashboard-shell.test.ts</automated>
<automated>npm run typecheck</automated>
</verification>

<success_criteria>
Plan 12-01 is complete when unsupported static `file://` loads produce a deliberate full-page blocker, `#root` remains a strict bootstrap contract, valid API-backed mode remains unblocked, and automated regressions prove the shell/runtime boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/12-dashboard-bootstrap-runtime-guardrails/12-01-SUMMARY.md`
</output>
