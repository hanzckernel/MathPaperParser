---
phase: 23-gcp-bootstrap-first-live-deployment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .gcloudignore
  - deploy/cloudrun/bootstrap.sh
  - deploy/cloudrun/build-image.sh
  - deploy/cloudrun/deploy.sh
  - deploy/cloudrun/service-metadata.sh
  - deploy/cloudrun/RUNBOOK.md
  - deploy/cloudrun/README.md
  - README.md
  - docs/deployment_readiness.md
  - packages/cli/test/cloud-run-bootstrap-contract.test.ts
autonomous: true
requirements:
  - GCP-01
  - GCP-02
must_haves:
  truths:
    - Phase 23 must add a repo-owned bootstrap path for the real GCP environment instead of relying on hidden console state.
    - The first live deployment must target the long-lived `paperparser` Cloud Run service in the configured project and region.
    - The operator path must surface exact deployed service metadata after deploy.
  artifacts:
    - path: deploy/cloudrun/bootstrap.sh
      provides: reproducible resource bootstrap/verification path
    - path: deploy/cloudrun/build-image.sh
      provides: repo-owned first live image build/publish path
    - path: deploy/cloudrun/service-metadata.sh
      provides: deployed service URL and revision metadata
    - path: packages/cli/test/cloud-run-bootstrap-contract.test.ts
      provides: mocked contract proof for bootstrap/build/metadata helpers
  key_links:
    - from: deploy/cloudrun/bootstrap.sh
      to: deploy/cloudrun/RUNBOOK.md
      via: resource bootstrap contract
      pattern: the runbook and helper agree on project, region, repository, runtime service account, and store bucket inputs
    - from: deploy/cloudrun/build-image.sh
      to: deploy/cloudrun/deploy.sh
      via: immutable image handoff
      pattern: the build helper emits the image URI the deploy helper consumes
    - from: deploy/cloudrun/deploy.sh
      to: deploy/cloudrun/service-metadata.sh
      via: live deployment outcome
      pattern: operators can fetch the service URL and latest ready revision after deploy
---

<objective>
Finish Phase 23 by making the first live GCP deploy reproducible from repo-owned helpers and by executing that path against the real PaperParser GCP project.

Purpose: turn the shipped Cloud Run hardening contract into a real deployed environment with bootstrap, build, deploy, and metadata surfaces that later CI/CD work can reuse.
Output: bootstrap/build/metadata helpers, aligned docs, mocked contract proof, and one real live deployment.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-gcp-bootstrap-first-live-deployment/23-CONTEXT.md
@deploy/cloudrun/deploy.sh
@deploy/cloudrun/grant-invoker.sh
@deploy/cloudrun/rollback.sh
@deploy/cloudrun/RUNBOOK.md
@deploy/cloudrun/README.md
@deploy/cloudrun/SMOKE.md
@README.md
@docs/deployment_readiness.md
@packages/cli/test/cloud-run-artifact.test.ts
@packages/cli/test/cloud-run-runbook-contract.test.ts
@packages/cli/test/cloud-run-security-contract.test.ts

Locked decisions to honor in this phase:
- Use checked-in bootstrap automation where practical.
- Reuse one long-lived Cloud Run service/environment.
- Preserve the shipped `v1.4` same-origin, authenticated, mounted-store contract.
- Do not depend on source-host or CI trigger wiring yet.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the bootstrap/build/metadata contract in failing tests</name>
  <files>packages/cli/test/cloud-run-bootstrap-contract.test.ts</files>
  <action>Add mocked gcloud regressions for resource bootstrap, Cloud Build image publishing, service metadata lookup, and `.gcloudignore` coverage before implementation.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-run-bootstrap-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New bootstrap/build/metadata tests fail before implementation.
    - The tests pin the real project/region helper contract and `.gcloudignore` exclusions.
  </acceptance_criteria>
  <done>The first-live-deploy contract is executable before helper code exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bootstrap, Cloud Build image publish, and metadata helpers</name>
  <files>.gcloudignore, deploy/cloudrun/bootstrap.sh, deploy/cloudrun/build-image.sh, deploy/cloudrun/deploy.sh, deploy/cloudrun/service-metadata.sh</files>
  <action>Add repo-owned helpers to bootstrap/verify the target GCP resources, build and publish the Cloud Run image through Cloud Build, preserve deploy semantics, and surface deployed service URL/revision metadata.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-run-bootstrap-contract.test.ts packages/cli/test/cloud-run-runbook-contract.test.ts packages/cli/test/cloud-run-security-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Bootstrap covers APIs, Artifact Registry, runtime service account, bucket access, and environment defaults.
    - Image publication uses Artifact Registry and produces an immutable URI suitable for deploy.
    - Service metadata is available from a repo-owned helper after deploy.
  </acceptance_criteria>
  <done>The repo contains a complete operator-driven first-live-deploy path.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Align operator docs and execute the real live deploy</name>
  <files>deploy/cloudrun/RUNBOOK.md, deploy/cloudrun/README.md, README.md, docs/deployment_readiness.md</files>
  <action>Update the docs to use the new bootstrap/build/deploy flow, then run the helpers against the real `paperparser-492322` project to produce the live service URL and latest ready revision metadata.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-run-bootstrap-contract.test.ts packages/cli/test/cloud-run-runbook-contract.test.ts packages/cli/test/cloud-run-security-contract.test.ts</automated>
    <automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
    <manual>export PATH=/opt/homebrew/bin:$PATH && PAPERPARSER_PROJECT=paperparser-492322 PAPERPARSER_REGION=europe-west1 PAPERPARSER_SERVICE=paperparser PAPERPARSER_ARTIFACT_REPOSITORY=paperparser PAPERPARSER_RUNTIME_SERVICE_ACCOUNT=paperparser-runtime@paperparser-492322.iam.gserviceaccount.com PAPERPARSER_STORE_BUCKET=paperparser-store-paperparser-492322 deploy/cloudrun/bootstrap.sh && PAPERPARSER_PROJECT=paperparser-492322 PAPERPARSER_REGION=europe-west1 PAPERPARSER_ARTIFACT_REPOSITORY=paperparser PAPERPARSER_IMAGE_TAG=$(git rev-parse --short HEAD) deploy/cloudrun/build-image.sh && PAPERPARSER_PROJECT=paperparser-492322 PAPERPARSER_REGION=europe-west1 PAPERPARSER_SERVICE=paperparser PAPERPARSER_IMAGE=europe-west1-docker.pkg.dev/paperparser-492322/paperparser/paperparser:$(git rev-parse --short HEAD) PAPERPARSER_RUNTIME_SERVICE_ACCOUNT=paperparser-runtime@paperparser-492322.iam.gserviceaccount.com PAPERPARSER_STORE_BUCKET=paperparser-store-paperparser-492322 deploy/cloudrun/deploy.sh && PAPERPARSER_PROJECT=paperparser-492322 PAPERPARSER_REGION=europe-west1 PAPERPARSER_SERVICE=paperparser deploy/cloudrun/service-metadata.sh</manual>
  </verify>
  <acceptance_criteria>
    - Docs describe the real project/region/service bootstrap and deploy flow with no hidden steps.
    - The live service is updated from the repo-owned path.
    - The operator outcome includes the service URL and latest ready revision.
  </acceptance_criteria>
  <done>The repo and the real GCP environment are aligned on one supported first-live-deploy path.</done>
</task>

</tasks>

<verification>
<automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-run-bootstrap-contract.test.ts packages/cli/test/cloud-run-runbook-contract.test.ts packages/cli/test/cloud-run-security-contract.test.ts</automated>
<automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
<manual>export PATH=/opt/homebrew/bin:$PATH && PAPERPARSER_PROJECT=paperparser-492322 PAPERPARSER_REGION=europe-west1 PAPERPARSER_SERVICE=paperparser PAPERPARSER_ARTIFACT_REPOSITORY=paperparser PAPERPARSER_RUNTIME_SERVICE_ACCOUNT=paperparser-runtime@paperparser-492322.iam.gserviceaccount.com PAPERPARSER_STORE_BUCKET=paperparser-store-paperparser-492322 deploy/cloudrun/bootstrap.sh && PAPERPARSER_PROJECT=paperparser-492322 PAPERPARSER_REGION=europe-west1 PAPERPARSER_ARTIFACT_REPOSITORY=paperparser PAPERPARSER_IMAGE_TAG=$(git rev-parse --short HEAD) deploy/cloudrun/build-image.sh && PAPERPARSER_PROJECT=paperparser-492322 PAPERPARSER_REGION=europe-west1 PAPERPARSER_SERVICE=paperparser PAPERPARSER_IMAGE=europe-west1-docker.pkg.dev/paperparser-492322/paperparser/paperparser:$(git rev-parse --short HEAD) PAPERPARSER_RUNTIME_SERVICE_ACCOUNT=paperparser-runtime@paperparser-492322.iam.gserviceaccount.com PAPERPARSER_STORE_BUCKET=paperparser-store-paperparser-492322 deploy/cloudrun/deploy.sh && PAPERPARSER_PROJECT=paperparser-492322 PAPERPARSER_REGION=europe-west1 PAPERPARSER_SERVICE=paperparser deploy/cloudrun/service-metadata.sh</manual>
</verification>

<success_criteria>
Plan 23-01 is complete when the repo ships a bootstrap/build/deploy/metadata helper set for the real GCP environment, the mocked helper tests and typecheck pass, and the `paperparser` Cloud Run service in `paperparser-492322` is updated from that repo-owned path with URL and revision metadata surfaced.
</success_criteria>

<output>
After completion, create `.planning/phases/23-gcp-bootstrap-first-live-deployment/23-01-SUMMARY.md`
</output>
