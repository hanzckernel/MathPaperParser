---
phase: 21-cloud-run-acceptance-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - deploy/cloudrun/SMOKE.md
  - README.md
  - docs/deployment_readiness.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements:
  - REL-03
  - REL-04
must_haves:
  truths:
    - The deployment-hardening milestone must be provable from one named command.
    - The acceptance bundle must cover packaging, same-origin runtime behavior, secure access scripts, and persistence/runbook helpers.
    - The docs must identify the same named proof operators should use.
  artifacts:
    - path: package.json
      provides: `test:acceptance:v1.4`
    - path: deploy/cloudrun/SMOKE.md
      provides: named smoke workflow note
    - path: README.md
      provides: top-level pointer to the named proof
  key_links:
    - from: package.json
      to: deploy/cloudrun/SMOKE.md
      via: shared proof command
      pattern: local acceptance and operator smoke note point at the same bundle
---

<objective>
Finish Phase 21 by publishing one named Cloud Run acceptance proof and one repo-visible smoke workflow note.

Purpose: turn the deployment milestone into a reproducible closeout proof instead of a pile of individually green tests.
Output: `npm run test:acceptance:v1.4`, smoke note, docs alignment, and verified execution.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@package.json
@README.md
@docs/deployment_readiness.md
@deploy/cloudrun/RUNBOOK.md
@packages/cli/test/serve-app.test.ts
@packages/web/test/bundle-data.test.ts
@packages/cli/test/cloud-run-artifact.test.ts
@packages/cli/test/cloud-run-security-contract.test.ts
@packages/cli/test/cloud-run-runbook-contract.test.ts

Locked decisions to honor in this phase:
- Use one named repo-local acceptance bundle.
- Reuse the focused phase tests instead of creating a heavyweight new harness.
- Pair the local acceptance bundle with a short smoke note for real-project follow-up.
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Publish the named acceptance bundle and smoke note</name>
  <files>package.json, deploy/cloudrun/SMOKE.md, README.md, docs/deployment_readiness.md</files>
  <action>Add `npm run test:acceptance:v1.4`, publish a Cloud Run smoke note, and update top-level docs so they point at the same named proof.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm run test:acceptance:v1.4</automated>
    <automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - One named command executes the deployment acceptance bundle.
    - The smoke note and top-level docs reference that same command.
    - The proof covers Phases 17-20 together.
  </acceptance_criteria>
  <done>The milestone has one named acceptance proof.</done>
</task>

</tasks>

<verification>
<automated>PATH=/opt/homebrew/bin:$PATH npm run test:acceptance:v1.4</automated>
<automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
</verification>

<success_criteria>
Plan 21-01 is complete when `npm run test:acceptance:v1.4` exists, runs green, and the docs identify it as the supported Cloud Run deployment proof for `v1.4`.
</success_criteria>

<output>
After completion, create `.planning/phases/21-cloud-run-acceptance-gate/21-01-SUMMARY.md`
</output>
