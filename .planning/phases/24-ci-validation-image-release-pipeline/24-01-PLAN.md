---
phase: 24-ci-validation-image-release-pipeline
plan: "01"
type: execute
wave: 1
depends_on:
  - "23-01"
files_modified:
  - package.json
  - cloudbuild.validate.yaml
  - cloudbuild.release.yaml
  - deploy/cloudrun/assert-mainline.sh
  - deploy/cloudrun/resolve-image-digest.sh
  - deploy/cloudrun/RUNBOOK.md
  - deploy/cloudrun/README.md
  - deploy/cloudrun/SMOKE.md
  - README.md
  - docs/deployment_readiness.md
  - packages/cli/test/cloud-build-pipeline-contract.test.ts
  - packages/cli/test/cloud-run-bootstrap-contract.test.ts
  - packages/cli/test/cloud-run-runbook-contract.test.ts
autonomous: true
requirements:
  - PIPE-01
  - PIPE-02
must_haves:
  truths:
    - Phase 24 must define a checked-in GCP-native Cloud Build pipeline contract rather than GitHub-host-specific workflows.
    - Validation must split into a faster gate and a heavier release gate.
    - Published images must use commit-SHA tags and surface the pushed digest as the canonical deployment identity.
    - Automated image publishing must be restricted to protected mainline only.
  artifacts:
    - path: cloudbuild.validate.yaml
      provides: fast validation gate contract
    - path: cloudbuild.release.yaml
      provides: release validation plus immutable image publish contract
    - path: deploy/cloudrun/assert-mainline.sh
      provides: mainline-only image publish guard
    - path: deploy/cloudrun/resolve-image-digest.sh
      provides: digest-backed immutable deployment identity after publish
    - path: packages/cli/test/cloud-build-pipeline-contract.test.ts
      provides: local proof for pipeline config, mainline guard, and digest resolution contract
  key_links:
    - from: cloudbuild.validate.yaml
      to: package.json
      via: fast validation gate
      pattern: the fast Cloud Build config must run the checked-in fast gate script rather than ad hoc commands
    - from: cloudbuild.release.yaml
      to: package.json
      via: release validation gate
      pattern: the release config must run the checked-in release gate script before any image publish step
    - from: cloudbuild.release.yaml
      to: deploy/cloudrun/assert-mainline.sh
      via: publish branch restriction
      pattern: release image publishing must fail outside the configured main branch
    - from: cloudbuild.release.yaml
      to: deploy/cloudrun/resolve-image-digest.sh
      via: immutable deployment identity
      pattern: the release pipeline must surface the pushed digest after publish for later deploy consumption
---

<objective>
Finish Phase 24 by defining the checked-in Cloud Build validation and image-release pipeline that later source integration and CD work will reuse.

Purpose: replace ad hoc release validation and image publication with repo-owned Cloud Build configs, split validation gates, digest-backed image identity, and mainline-only publishing.
Output: Cloud Build configs, helper scripts, docs alignment, contract tests, and updated milestone state.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/24-ci-validation-image-release-pipeline/24-CONTEXT.md
@package.json
@Dockerfile
@deploy/cloudrun/build-image.sh
@deploy/cloudrun/deploy.sh
@deploy/cloudrun/RUNBOOK.md
@deploy/cloudrun/README.md
@deploy/cloudrun/SMOKE.md
@README.md
@docs/deployment_readiness.md
@packages/cli/test/cloud-run-artifact.test.ts
@packages/cli/test/cloud-run-bootstrap-contract.test.ts
@packages/cli/test/cloud-run-runbook-contract.test.ts
@packages/cli/test/cloud-run-security-contract.test.ts

Locked decisions to honor in this phase:
- Use Cloud Build as the primary checked-in pipeline contract.
- Split validation into a faster CI gate and a heavier release gate.
- Publish commit-SHA-tagged images and capture the pushed digest as canonical identity.
- Restrict automated image publishing to protected mainline only.
- Treat stale sample artifacts only as release-input hygiene, not as broader product scope.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the Phase 24 pipeline contract in failing tests</name>
  <files>packages/cli/test/cloud-build-pipeline-contract.test.ts, packages/cli/test/cloud-run-bootstrap-contract.test.ts, packages/cli/test/cloud-run-runbook-contract.test.ts</files>
  <action>Add failing regressions for the fast/release Cloud Build configs, mainline publish guard, digest resolution helper, docs alignment, and release-input hygiene boundaries.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-build-pipeline-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New tests fail before the Cloud Build configs and helpers exist.
    - Tests pin the split validation gate, mainline-only publishing, SHA-tagged image contract, digest resolution, and release docs surface.
  </acceptance_criteria>
  <done>The Phase 24 contract is explicit before implementation starts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Cloud Build configs, gate scripts, and digest helper</name>
  <files>package.json, cloudbuild.validate.yaml, cloudbuild.release.yaml, deploy/cloudrun/assert-mainline.sh, deploy/cloudrun/resolve-image-digest.sh</files>
  <action>Add the checked-in fast/release gate scripts, Cloud Build definitions, mainline guard, and digest-resolution helper needed for immutable image publishing.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-build-pipeline-contract.test.ts packages/cli/test/cloud-run-bootstrap-contract.test.ts packages/cli/test/cloud-run-runbook-contract.test.ts packages/cli/test/cloud-run-security-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Fast and release validation gates are named package scripts and reused by Cloud Build.
    - Release publish uses commit-SHA tags and resolves a fully qualified digest after push.
    - Publish attempts fail outside the configured mainline ref.
  </acceptance_criteria>
  <done>The repo owns a stable Cloud Build validation and image-release contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Align deployment docs and verify the pipeline contract locally</name>
  <files>deploy/cloudrun/RUNBOOK.md, deploy/cloudrun/README.md, deploy/cloudrun/SMOKE.md, README.md, docs/deployment_readiness.md</files>
  <action>Update the docs to point operators and maintainers at the checked-in Cloud Build configs, the split gate scripts, the mainline-only publish rule, and the digest-backed image handoff for later deploy steps.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-build-pipeline-contract.test.ts packages/cli/test/cloud-run-bootstrap-contract.test.ts packages/cli/test/cloud-run-runbook-contract.test.ts packages/cli/test/cloud-run-security-contract.test.ts packages/cli/test/cloud-run-artifact.test.ts</automated>
    <automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Docs no longer present image publication as an ad hoc manual process.
    - The checked-in Cloud Build configs are named as the supported validation/image-release contract.
    - The docs explain that deploy steps must consume the pushed digest rather than rebuild.
  </acceptance_criteria>
  <done>The repo, tests, and docs agree on the first CI/image-release contract.</done>
</task>

</tasks>

<verification>
<automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-build-pipeline-contract.test.ts packages/cli/test/cloud-run-bootstrap-contract.test.ts packages/cli/test/cloud-run-runbook-contract.test.ts packages/cli/test/cloud-run-security-contract.test.ts packages/cli/test/cloud-run-artifact.test.ts</automated>
<automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
</verification>

<success_criteria>
Plan 24-01 is complete when the repo ships checked-in Cloud Build configs for fast validation and release image publication, the publish path is mainline-only and digest-backed, docs point to the new pipeline contract, and the local contract tests plus typecheck pass.
</success_criteria>

<output>
After completion, create `.planning/phases/24-ci-validation-image-release-pipeline/24-01-SUMMARY.md`
</output>
