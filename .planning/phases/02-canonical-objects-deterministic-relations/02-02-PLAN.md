---
phase: 02-canonical-objects-deterministic-relations
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - packages/core/src/types/node.ts
  - packages/core/src/types/edge.ts
  - packages/core/src/types/bundle.ts
  - packages/core/src/graph/stats.ts
  - packages/core/src/serialization/bundle-serializer.ts
  - packages/core/src/validation/consistency-checker.ts
  - schema/graph.schema.json
  - schema/index.schema.json
  - packages/web/src/lib/dashboard-model.ts
  - packages/web/src/components/proof-graph-page.tsx
autonomous: true
requirements:
  - OBJ-02
  - OBJ-03
  - REL-03
  - REL-04
must_haves:
  truths:
    - Phase 2 contract changes must move through types, serializer, schema, validator, stats, and compatibility consumers together.
    - The contract remains backward-compatible enough to load current published example bundles and existing stores.
    - Provenance becomes distinct from evidence on edges without forcing a second graph representation.
  artifacts:
    - path: packages/core/src/types/node.ts
      provides: Expanded node kinds plus typed source anchors
    - path: packages/core/src/types/edge.ts
      provides: Edge provenance model alongside existing evidence detail
    - path: packages/core/src/serialization/bundle-serializer.ts
      provides: Contract round-trip for anchors and provenance with legacy compatibility
    - path: schema/graph.schema.json
      provides: Serialized graph contract for expanded canonical objects and relations
    - path: packages/web/src/components/proof-graph-page.tsx
      provides: Compatibility with expanded node kinds and provenance-bearing edges
---

<objective>
Implement the Phase 2 contract plumbing without yet relying on parser population details.

Purpose: Make the bundle model capable of representing first-class canonical objects, source anchors, and provenance-bearing deterministic relations across every consumer that reads or validates the bundle.
Output: Updated types, schema, serializer, stats, validator, and web compatibility layer that can carry the expanded contract coherently.
</objective>

<context>
@.planning/phases/02-canonical-objects-deterministic-relations/02-CONTEXT.md
@.planning/phases/02-canonical-objects-deterministic-relations/02-RESEARCH.md
@.planning/phases/02-canonical-objects-deterministic-relations/02-VALIDATION.md
@packages/core/src/types/node.ts
@packages/core/src/types/edge.ts
@packages/core/src/types/bundle.ts
@packages/core/src/graph/stats.ts
@packages/core/src/serialization/bundle-serializer.ts
@packages/core/src/validation/consistency-checker.ts
@packages/web/src/lib/dashboard-model.ts
@packages/web/src/components/proof-graph-page.tsx
@schema/graph.schema.json
@schema/index.schema.json

Locked decisions to honor in this phase:
- D-01: Keep `external_dependency` as the Phase 2 citation object model.
- D-02: Serialize node anchors using the existing `filePath`, `startLine`, and `endLine` fields rather than inventing a sidecar anchor object.
- D-03: Add edge provenance separately from evidence; do not collapse them into one overloaded field.
- D-04: Preserve legacy schema-example loading and existing store compatibility while new bundles emit the richer contract.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the canonical node and edge contract with anchor and provenance support</name>
  <files>packages/core/src/types/node.ts, packages/core/src/types/edge.ts, packages/core/src/types/bundle.ts, packages/core/src/graph/stats.ts, packages/core/src/serialization/bundle-serializer.ts, packages/core/src/validation/consistency-checker.ts, schema/graph.schema.json, schema/index.schema.json</files>
  <action>Add node kinds for section/proof/equation, add a provenance enum/field for edges, and round-trip node anchors through serialization. Update schema and consistency logic so new bundles can emit the richer contract while legacy example bundles can still be loaded and validated. Keep evidence as the explanation basis already used by the system; provenance is additive. Update bundle stats and consistency logic only as far as required by the new contract.</action>
  <verify>
    <automated>npx vitest run packages/core/test/bundle-serializer.test.ts packages/core/test/validation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - New bundles can represent section/proof/equation nodes and edge provenance without schema or serializer drift.
    - Source anchors survive `BundleSerializer` round-trip.
    - Legacy example bundles still load through the serializer and validator path.
  </acceptance_criteria>
  <done>Canonical contract plumbing supports the expanded node/edge model and passes its targeted tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make web compatibility consumers tolerate the expanded canonical contract</name>
  <files>packages/web/src/lib/dashboard-model.ts, packages/web/src/components/proof-graph-page.tsx</files>
  <action>Update the dashboard data model and proof-graph compatibility layer so new node kinds and provenance-bearing edges do not break loading or rendering. Keep the work scoped to compatibility, not new Phase 3 UI behavior. Ensure default coloring/filter behavior remains coherent when new kinds arrive.</action>
  <verify>
    <automated>npx vitest run packages/web/test/bundle-data.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Serialized bundles with section/proof/equation nodes load into the dashboard model cleanly.
    - The proof graph component does not assume the old fixed node-kind set or the absence of provenance.
    - No Phase 3-only UX work is introduced.
  </acceptance_criteria>
  <done>Compatibility consumers are ready for parser-populated Phase 2 data.</done>
</task>

</tasks>

<verification>
<automated>npx vitest run packages/core/test/bundle-serializer.test.ts packages/core/test/validation.test.ts packages/web/test/bundle-data.test.ts</automated>
</verification>

<success_criteria>
Plan 02-02 is complete when the canonical bundle contract, serializer, validator, stats, and compatibility consumers can all represent the Phase 2 object/anchor/provenance model coherently without breaking legacy example loading.
</success_criteria>

<output>
After completion, create `.planning/phases/02-canonical-objects-deterministic-relations/02-02-SUMMARY.md`
</output>
