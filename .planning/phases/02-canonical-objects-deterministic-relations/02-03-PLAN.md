---
phase: 02-canonical-objects-deterministic-relations
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - packages/core/src/ingestion/flatten/latex-flattener.ts
  - packages/core/src/ingestion/parsers/latex-parser.ts
  - packages/core/src/ingestion/parsers/markdown-parser.ts
  - packages/core/src/ingestion/bundle-builder.ts
  - packages/core/test/gold-paper-canonical-artifact.test.ts
  - packages/core/test/ingestion-pipeline.test.ts
  - packages/core/test/latex-flattener.test.ts
autonomous: true
requirements:
  - OBJ-01
  - OBJ-02
  - OBJ-03
  - REL-01
  - REL-02
  - REL-03
  - REL-04
  - ACC-02
must_haves:
  truths:
    - Parser population must stay on the existing local ingestion path; no second artifact pipeline is introduced.
    - Gold-paper acceptance remains the source of truth for whether Phase 2 is actually complete.
    - Structural relations are deterministic and must be emitted explicitly, not inferred later in UI code.
  artifacts:
    - path: packages/core/src/ingestion/flatten/latex-flattener.ts
      provides: Flattened line mapping sufficient to recover source anchors
    - path: packages/core/src/ingestion/parsers/latex-parser.ts
      provides: First-class canonical section/proof/equation objects plus explicit and structural deterministic edges
    - path: packages/core/src/ingestion/parsers/markdown-parser.ts
      provides: Contract-compatible output for markdown inputs after Phase 2 expansion
    - path: packages/core/test/gold-paper-canonical-artifact.test.ts
      provides: End-to-end acceptance proof on the representative paper
---

<objective>
Populate the expanded canonical contract from real parser output and prove it on the gold paper.

Purpose: Turn the contract work from Plan 02-02 into real deterministic bundle contents on both the reduced fixture and the representative gold paper.
Output: Gold-paper bundles with first-class canonical objects, source anchors, structural relations, provenance-bearing deterministic edges, and stable reruns.
</objective>

<context>
@.planning/phases/02-canonical-objects-deterministic-relations/02-CONTEXT.md
@.planning/phases/02-canonical-objects-deterministic-relations/02-RESEARCH.md
@.planning/phases/02-canonical-objects-deterministic-relations/02-VALIDATION.md
@packages/core/src/ingestion/flatten/latex-flattener.ts
@packages/core/src/ingestion/parsers/latex-parser.ts
@packages/core/src/ingestion/parsers/markdown-parser.ts
@packages/core/src/ingestion/bundle-builder.ts
@packages/core/test/gold-paper-canonical-artifact.test.ts
@packages/core/test/ingestion-pipeline.test.ts
@packages/core/test/latex-flattener.test.ts
@ref/papers/long_nalini/arXiv-2502.12268v2/main.tex

Locked decisions to honor in this phase:
- D-01: Source anchors must point back to original TeX files and line spans, not just flattened output.
- D-02: Structural edges such as containment and proof attachment must be emitted by the parser/builder, not synthesized in the UI.
- D-03: The gold paper's remaining unresolved references may stay as diagnostics, but deterministic edges should still cover explicit resolvable refs and structural links.
- D-04: ACC-02 requires rerun stability after normalizing explicitly time-varying metadata only.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add source-line mapping and parser population for first-class canonical objects</name>
  <files>packages/core/src/ingestion/flatten/latex-flattener.ts, packages/core/src/ingestion/parsers/latex-parser.ts, packages/core/src/ingestion/parsers/markdown-parser.ts, packages/core/src/ingestion/bundle-builder.ts</files>
  <action>Extend the LaTeX flattener with sufficient line/source mapping to let parsers recover original file anchors. Update the LaTeX parser to emit section, proof, equation, and citation-object nodes with stable IDs and anchors, plus deterministic explicit and structural edges carrying provenance and explanatory detail. Keep markdown parser output contract-compatible after the node/edge expansion. Update bundle-builder behavior only as needed so main-result, summary, and stats logic remains coherent with the new canonical objects.</action>
  <verify>
    <automated>npx vitest run packages/core/test/gold-paper-canonical-artifact.test.ts packages/core/test/ingestion-pipeline.test.ts packages/core/test/latex-flattener.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Reduced fixture tests go green with anchored section/proof/equation objects and structural relations.
    - Gold-paper output contains section/proof/equation/external dependency nodes and provenance-bearing deterministic edges.
    - Existing Phase 1 guarantees remain intact.
  </acceptance_criteria>
  <done>Real parser output populates the full Phase 2 canonical contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove gold-paper acceptance and deterministic rerun stability for the expanded canonical artifact</name>
  <files>packages/core/test/gold-paper-canonical-artifact.test.ts</files>
  <action>Finalize the gold-paper contract test and acceptance commands so the representative paper proves OBJ/REL coverage and ACC-02. Compare two analyze runs after normalizing timestamp-like metadata and assert stable canonical IDs, node/edge counts, and provenance-bearing edge shapes. Keep the acceptance path on the built CLI and stored bundle output.</action>
  <verify>
    <automated>npx vitest run packages/core/test/gold-paper-canonical-artifact.test.ts</automated>
    <automated>npm run build --workspace @paperparser/cli</automated>
    <automated>node packages/cli/dist/index.js analyze ref/papers/long_nalini/arXiv-2502.12268v2/main.tex --store /tmp/paperparser-phase2-acceptance --paper long-nalini</automated>
    <automated>node packages/cli/dist/index.js analyze ref/papers/long_nalini/arXiv-2502.12268v2/main.tex --store /tmp/paperparser-phase2-acceptance-rerun --paper long-nalini</automated>
    <automated>node packages/cli/dist/index.js validate --store /tmp/paperparser-phase2-acceptance --paper long-nalini --json</automated>
    <automated>npm test</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Gold-paper acceptance passes on the built CLI using the expanded canonical artifact.
    - Rerun comparison proves stable canonical output apart from explicitly time-varying metadata.
    - Full suite and typecheck stay green after the contract expansion.
  </acceptance_criteria>
  <done>Phase 2 acceptance is proven on the representative paper and the repo is green.</done>
</task>

</tasks>

<verification>
<automated>npx vitest run packages/core/test/gold-paper-canonical-artifact.test.ts packages/core/test/ingestion-pipeline.test.ts packages/core/test/bundle-serializer.test.ts packages/core/test/validation.test.ts packages/web/test/bundle-data.test.ts</automated>
<automated>npm test</automated>
<automated>npm run typecheck</automated>
</verification>

<success_criteria>
Plan 02-03 is complete when the representative gold paper produces a stable canonical artifact with first-class objects, source anchors, deterministic explicit and structural relations, provenance-bearing edges, and green full-suite verification.
</success_criteria>

<output>
After completion, create `.planning/phases/02-canonical-objects-deterministic-relations/02-03-SUMMARY.md`
</output>
