---
phase: 15-math-fragment-render-hardening
plan: "01"
type: execute
wave: 1
depends_on:
  - 14-01
files_modified:
  - packages/web/src/lib/math-render.tsx
  - packages/web/test/math-render.test.ts
  - packages/web/test/proof-graph-render.test.ts
autonomous: true
requirements:
  - MATH-04
  - MATH-05
  - MATH-06
must_haves:
  truths:
    - Phase 15 must reduce raw-source fallbacks on accepted-corpus representative statements through render-time normalization only.
    - The new normalization must stay project-owned and bounded rather than relying on browser-side `amsmath` / `amsthm` rescue behavior.
    - Unsupported or unclear fragments must still degrade explicitly instead of being silently coerced into misleading output.
  artifacts:
    - path: packages/web/src/lib/math-render.tsx
      provides: Expanded normalization pipeline for accepted-corpus-first fragment classes plus explicit fallback decisions
    - path: packages/web/test/math-render.test.ts
      provides: Direct regressions for list flattening, bounded display salvage, wrapper normalization, and explicit fallback preservation
    - path: packages/web/test/proof-graph-render.test.ts
      provides: Surface-level proof that the existing statement-reading views still route through the shared math-render boundary after the normalization expansion
  key_links:
    - from: packages/web/src/lib/math-render.tsx
      to: packages/web/test/math-render.test.ts
      via: accepted-corpus-first fragment regressions
      pattern: representative statement fragments normalize to typeset output instead of raw-source fallback
    - from: packages/web/src/lib/math-render.tsx
      to: packages/web/test/proof-graph-render.test.ts
      via: shared statement-surface wiring
      pattern: theorem explorer and proof-graph detail views still use the same render boundary
---

<objective>
Finish Phase 15 by expanding the existing MathJax normalization boundary so more extracted statement fragments typeset safely on the current reading surfaces.

Purpose: lower accepted-corpus fallback frequency and improve readability for the next bounded fragment classes without mutating canonical bundle text or overstating browser compatibility.
Output: accepted-corpus-first normalization for list-like and wrapper-heavy fragments, one adjacent display-salvage class, preserved explicit fallback for out-of-scope environments, and verified statement-surface wiring.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-math-fragment-render-hardening/15-CONTEXT.md
@.planning/phases/15-math-fragment-render-hardening/15-RESEARCH.md
@.planning/phases/11-dashboard-math-rendering-repair/11-CONTEXT.md
@packages/web/src/lib/math-render.tsx
@packages/web/test/math-render.test.ts
@packages/web/test/proof-graph-render.test.ts

Locked decisions to honor in this phase:
- Accepted-corpus failures come first; only one adjacent reusable class should be absorbed if it falls out naturally.
- Prefer aggressive `$$ ... $$` display salvage when the shaped output is still readable and honest.
- Expand readable command rewrites, but do not drift into broad browser-side package emulation.
- Keep the same statement-bearing surfaces only; do not broaden MathJax coverage across the app.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture accepted-corpus-first render gaps in failing regressions</name>
  <files>packages/web/test/math-render.test.ts, packages/web/test/proof-graph-render.test.ts</files>
  <action>Add direct regressions for the main accepted-corpus fallback drivers: list-environment fragments, wrapper-heavy readable fragments, and one bounded display-salvage class. Keep an explicit fallback regression for an out-of-scope environment such as `figure` or `tikzcd`.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/web/test/math-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - At least one new regression fails before the render helper changes exist.
    - The tests use accepted-corpus-representative fragment shapes rather than only synthetic toy math.
    - The regressions distinguish newly supported fragments from deliberately unsupported fallback cases.
  </acceptance_criteria>
  <done>The approved Phase 15 render contract exists as executable regressions before production edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand the MathJax normalization pipeline in a bounded way</name>
  <files>packages/web/src/lib/math-render.tsx</files>
  <action>Refactor `prepareMathStatementText()` into an ordered normalization pass that flattens readable list environments, strips bounded text/spacing wrappers, salvages one adjacent display-like fragment class into a render-safe shape, and preserves explicit fallback for out-of-scope environments.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/web/test/math-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Representative `itemize` / `enumerate` fragments typeset instead of falling back.
    - Wrapper-heavy fragments become more readable without relying on browser package addons.
    - The chosen adjacent display-like class is normalized into a defensible render-safe form.
    - `figure` and similarly unclear environments still produce explicit fallback rather than silent coercion.
  </acceptance_criteria>
  <done>The shared render helper supports the next bounded accepted-corpus fragment classes without broad compatibility claims.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Re-prove shared surface wiring and workspace safety</name>
  <files>packages/web/test/proof-graph-render.test.ts</files>
  <action>Confirm that the theorem explorer and proof-graph detail views still mount the shared math-render boundary after the normalization expansion, then run workspace typecheck.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/web/test/proof-graph-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Both existing statement-reading surfaces still render through `MathTextBlock`.
    - No surface-level contract regression appears while the helper logic expands.
    - Workspace typecheck stays green.
  </acceptance_criteria>
  <done>The new normalization remains wired through the existing dashboard surfaces and does not introduce TypeScript regressions.</done>
</task>

</tasks>

<verification>
<automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/web/test/math-render.test.ts</automated>
<automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/web/test/proof-graph-render.test.ts</automated>
<automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
</verification>

<success_criteria>
Plan 15-01 is complete when accepted-corpus-first statement fragments that previously fell back now normalize into readable typeset output, out-of-scope environments still fail explicitly, and the current theorem-reading surfaces remain wired to the shared MathJax boundary with passing automated verification.
</success_criteria>

<output>
After completion, create `.planning/phases/15-math-fragment-render-hardening/15-01-SUMMARY.md`
</output>
