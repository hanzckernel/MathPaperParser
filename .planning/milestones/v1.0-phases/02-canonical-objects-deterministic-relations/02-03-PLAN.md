---
phase: 02-canonical-objects-deterministic-relations
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - packages/core/src/graph/knowledge-graph.ts
  - packages/core/src/services/bundle-query-service.ts
  - packages/core/test/knowledge-graph.test.ts
  - packages/core/test/query-service.test.ts
  - packages/core/test/gold-paper-canonical-stability.test.ts
  - packages/cli/test/analyze-command.test.ts
  - packages/cli/test/read-commands.test.ts
  - packages/mcp/test/server.test.ts
autonomous: true
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-04
  - ACC-02
must_haves:
  truths:
    - Query, context, impact, and proof-chain surfaces remain focused on mathematical dependency edges instead of being swamped by structural section/proof links.
    - Stored CLI and MCP bundle surfaces accept the richer graph and keep anchors plus provenance visible and schema-valid.
    - Re-running deterministic parsing on the same gold paper produces a canonical bundle that is byte-stable after stripping the explicitly volatile run-metadata fields.
  artifacts:
    - path: packages/core/src/graph/knowledge-graph.ts
      provides: Traversal rules that keep structural edges stored but out of dependency-chain semantics
    - path: packages/core/src/services/bundle-query-service.ts
      provides: Query/context/impact behavior that stays theorem-centric with Phase 2 structural edges present
    - path: packages/core/test/gold-paper-canonical-stability.test.ts
      provides: ACC-02 automated rerun acceptance on the real gold paper
    - path: packages/cli/test/read-commands.test.ts
      provides: Stored CLI regression for query/context/impact after structural edges land
    - path: packages/mcp/test/server.test.ts
      provides: MCP regression for stored Phase 2 bundles with anchors and provenance
  key_links:
    - from: packages/core/src/graph/knowledge-graph.ts
      to: packages/core/src/services/bundle-query-service.ts
      via: dependency traversal and impact collection
      pattern: getDependencyChain
    - from: packages/cli/test/read-commands.test.ts
      to: packages/core/src/services/bundle-query-service.ts
      via: CLI `context` and `impact` JSON output
      pattern: outgoingEdges
    - from: packages/core/test/gold-paper-canonical-stability.test.ts
      to: ref/papers/long_nalini/arXiv-2502.12268v2/main.tex
      via: normalized rerun comparison for ACC-02
      pattern: analyzeDocumentPath
---

<objective>
Protect theorem-centric query semantics, stored bundle surfaces, and deterministic rerun acceptance after the Phase 2 graph becomes richer.

Purpose: Finish Phase 2 by proving that the richer canonical graph does not break query/store/MCP behavior and that deterministic reruns on the gold paper are stable under an explicit normalization rule.
Output: Query traversal guardrails, stored-surface regressions, and an ACC-02 gold-paper stability test that compare canonical output rather than loose counts.
</objective>

<execution_context>
@/Users/hanzhicheng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hanzhicheng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/01-gold-paper-tex-ingestion-hardening/01-01-SUMMARY.md
@.planning/phases/01-gold-paper-tex-ingestion-hardening/01-02-SUMMARY.md
@.planning/phases/02-canonical-objects-deterministic-relations/02-CONTEXT.md
@.planning/phases/02-canonical-objects-deterministic-relations/02-RESEARCH.md
@packages/core/src/graph/knowledge-graph.ts
@packages/core/src/services/bundle-query-service.ts
@packages/core/test/knowledge-graph.test.ts
@packages/core/test/query-service.test.ts
@packages/cli/test/analyze-command.test.ts
@packages/cli/test/read-commands.test.ts
@packages/mcp/test/server.test.ts
@ref/papers/long_nalini/arXiv-2502.12268v2/main.tex

Locked decisions to honor in this phase:
- D-01: Preserve the canonical `manifest` / `graph` / `index` bundle architecture and keep the public contract additive at `0.2.0`.
- D-02: Deterministic parsing remains the trusted baseline; Phase 2 must not introduce agent-inferred semantics into the canonical artifact.
- D-03: Treat `external_dependency` as the Phase 2 citation object model; do not add a separate `citation` node kind unless implementation proves it is impossible.
- D-04: Source anchors must be the serialized `filePath`, `startLine`, and `endLine` fields already present on `MathNode`, not a new sidecar anchor system.
- D-05: Separate edge provenance from evidence so deterministic relations can carry both machine-filterable provenance and user-facing explanatory evidence.
- D-06: Keep Phase 2 scoped to canonical artifact quality and compatibility; no Phase 3 explorer workflow redesign and no Phase 4 enrichment storage.

<interfaces>
From `packages/core/src/services/bundle-query-service.ts`:
```ts
export class BundleQueryService {
  search(query: SearchQuery): SearchResult[];
  getContext(nodeId: string): NodeContext;
  getImpact(nodeId: string): ImpactAnalysis;
}
```

From `packages/core/src/graph/knowledge-graph.ts`:
```ts
export class MathKnowledgeGraph {
  getEdges(nodeId: string, direction: 'in' | 'out' | 'both'): MathEdge[];
  getDependencyChain(nodeId: NodeId, depth?: number): MathNode[];
  getProofFlow(nodeId: NodeId): MathNode[];
  computeStats(): BundleStats;
}
```

From `packages/cli/src/index.ts`:
```ts
export function runCli(argv: string[], io?: CliIo): number;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Keep structural edges stored without letting them break dependency traversal and impact semantics</name>
  <files>packages/core/src/graph/knowledge-graph.ts, packages/core/src/services/bundle-query-service.ts, packages/core/test/knowledge-graph.test.ts, packages/core/test/query-service.test.ts</files>
  <read_first>
    packages/core/src/graph/knowledge-graph.ts
    packages/core/src/services/bundle-query-service.ts
    packages/core/test/knowledge-graph.test.ts
    packages/core/test/query-service.test.ts
  </read_first>
  <behavior>
    - Raw node context can still expose stored `contains` and `proves` edges when the user inspects the graph.
    - `dependencyChain`, `proofFlow`, and impact traversal stay focused on mathematical dependency edge kinds instead of section/proof bookkeeping.
    - Existing theorem-centric query expectations remain stable even though the stored graph is richer.
  </behavior>
  <action>Per D-02, D-05, and D-06, keep structural edges in the canonical graph but fence them out of theorem-dependency traversal by default. Update `packages/core/src/graph/knowledge-graph.ts` with a clear allowlist for mathematical dependency traversal, including `uses_in_proof`, `extends`, `generalizes`, `specializes`, `equivalent_to`, and `cites_external`, while leaving `contains` and `proves` available through raw `getEdges()`. Update `packages/core/src/services/bundle-query-service.ts` so `getContext()`, `getImpact()`, and proof-flow/dependency-chain behavior stay useful for theorem-like navigation after Phase 2, rather than returning sections or proof wrapper nodes as if they were semantic dependencies. Adjust `packages/core/test/knowledge-graph.test.ts` and `packages/core/test/query-service.test.ts` to lock this rule in explicitly: structural edges remain stored and inspectable, but dependency traversal and reverse impact remain mathematical.</action>
  <verify>
    <automated>npx vitest run packages/core/test/knowledge-graph.test.ts packages/core/test/query-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Structural edges are visible in raw edge lists but excluded from dependency-chain traversal unless a future caller opts in explicitly.
    - Theorem-oriented query and impact results stay stable on existing markdown fixtures after structural edges are added to the graph.
    - The implementation adds one obvious traversal rule rather than scattering ad hoc `kind !== 'contains'` checks across multiple call sites.
  </acceptance_criteria>
  <done>Phase 2 structural edges no longer threaten existing dependency/query semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove stored-surface compatibility and normalized gold-paper rerun stability for ACC-02</name>
  <files>packages/core/test/gold-paper-canonical-stability.test.ts, packages/cli/test/analyze-command.test.ts, packages/cli/test/read-commands.test.ts, packages/mcp/test/server.test.ts</files>
  <read_first>
    packages/core/test/gold-paper-ingestion.test.ts
    packages/cli/test/analyze-command.test.ts
    packages/cli/test/read-commands.test.ts
    packages/mcp/test/server.test.ts
    ref/papers/long_nalini/arXiv-2502.12268v2/main.tex
  </read_first>
  <behavior>
    - The real gold paper can be analyzed twice into canonical bundles that are deeply equal after normalizing only `manifest.created_at`, `manifest.producer.timestamp_start`, and `manifest.producer.timestamp_end`.
    - Stored CLI and MCP surfaces keep working with bundles that contain anchors, `section` / `proof` / `equation` nodes, structural edges, and provenance-aware stats.
    - Stored validations remain green and do not require a schema-version bump or a custom compatibility mode.
  </behavior>
  <action>Per D-01 through D-05, add a concrete ACC-02 acceptance harness instead of relying on loose counts. Create `packages/core/test/gold-paper-canonical-stability.test.ts` that runs `analyzeDocumentPath()` twice on `ref/papers/long_nalini/arXiv-2502.12268v2/main.tex`, serializes both bundles, strips only `manifest.created_at`, `manifest.producer.timestamp_start`, and `manifest.producer.timestamp_end`, and asserts deep equality of the resulting `manifest`, `graph`, and `index`. If diagnostics are included in the test, compare them after a stable sort by warning code, source path, and metadata so nondeterministic ordering cannot hide a real regression. Extend `packages/cli/test/analyze-command.test.ts` and `packages/cli/test/read-commands.test.ts` so stored bundles are asserted to contain anchors, new kinds, `provenance`, and updated stats while `validate`, `context`, and `impact` remain useful. Extend `packages/mcp/test/server.test.ts` so `validate_bundle`, `get_context`, `impact_analysis`, and stored resource reads still work on Phase 2 bundles without dropping the new fields.</action>
  <verify>
    <automated>npx vitest run packages/core/test/gold-paper-canonical-stability.test.ts packages/cli/test/analyze-command.test.ts packages/cli/test/read-commands.test.ts packages/mcp/test/server.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - ACC-02 is enforced by a real automated rerun test on the representative gold paper, not by a one-off manual diff.
    - CLI and MCP tests prove that stored bundles retain Phase 2 anchors, provenance, and structural edges while remaining schema-valid.
    - The normalization rule for rerun stability is explicit and narrow enough that future nondeterminism cannot be hand-waved away as “metadata.”
  </acceptance_criteria>
  <done>Phase 2 is acceptance-tested end-to-end on query/store/MCP surfaces and on deterministic gold-paper reruns.</done>
</task>

</tasks>

<verification>
The acceptance wave is complete when theorem-centric query semantics still hold, stored surfaces accept the richer bundle, and gold-paper deterministic reruns are stable after explicit metadata normalization.

<automated>npx vitest run packages/core/test/knowledge-graph.test.ts packages/core/test/query-service.test.ts packages/core/test/gold-paper-canonical-stability.test.ts packages/cli/test/analyze-command.test.ts packages/cli/test/read-commands.test.ts packages/mcp/test/server.test.ts</automated>
<automated>npm run build</automated>
<automated>node packages/cli/dist/index.js analyze ref/papers/long_nalini/arXiv-2502.12268v2/main.tex --store /tmp/paperparser-phase2-acceptance-a --paper long-nalini</automated>
<automated>node packages/cli/dist/index.js analyze ref/papers/long_nalini/arXiv-2502.12268v2/main.tex --store /tmp/paperparser-phase2-acceptance-b --paper long-nalini</automated>
<automated>node packages/cli/dist/index.js validate --store /tmp/paperparser-phase2-acceptance-a --paper long-nalini --json</automated>
<automated>node -e "const fs=require('node:fs'); const path=require('node:path'); const normalize=(bundle)=>({manifest:{...bundle.manifest,created_at:'<normalized>',producer:{...bundle.manifest.producer,timestamp_start:'<normalized>',timestamp_end:'<normalized>'}},graph:bundle.graph,index:bundle.index}); const read=(base)=>({manifest:JSON.parse(fs.readFileSync(path.join(base,'manifest.json'),'utf8')),graph:JSON.parse(fs.readFileSync(path.join(base,'graph.json'),'utf8')),index:JSON.parse(fs.readFileSync(path.join(base,'index.json'),'utf8'))}); const first=normalize(read('/tmp/paperparser-phase2-acceptance-a/long-nalini')); const second=normalize(read('/tmp/paperparser-phase2-acceptance-b/long-nalini')); if (JSON.stringify(first)!==JSON.stringify(second)) throw new Error('normalized canonical bundle differs across deterministic reruns'); console.log(JSON.stringify({nodeCount:first.graph.nodes.length,edgeCount:first.graph.edges.length,provenance:first.index.stats.provenance_breakdown}, null, 2));"</automated>
<automated>npm test</automated>
<automated>npm run typecheck</automated>
</verification>

<success_criteria>
Plan 02-03 is complete when the richer Phase 2 graph does not break theorem-centric query/store/MCP behavior and the gold-paper canonical artifact is stable across deterministic reruns after stripping only the explicit run-timestamp fields.
</success_criteria>

<output>
After completion, create `.planning/phases/02-canonical-objects-deterministic-relations/02-03-SUMMARY.md`
</output>
