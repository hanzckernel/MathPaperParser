---
phase: 02-canonical-objects-deterministic-relations
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - packages/core/src/ingestion/parsers/latex-parser.ts
  - packages/core/src/ingestion/parsers/markdown-parser.ts
  - packages/core/src/ingestion/bundle-builder.ts
  - packages/core/test/fixtures/latex/gold-paper-relations/proof-equation.tex
  - packages/core/test/ingestion-pipeline.test.ts
  - packages/core/test/markdown-parser.test.ts
  - packages/core/test/gold-paper-ingestion.test.ts
  - packages/web/src/lib/dashboard-model.ts
  - packages/web/src/components/proof-graph-page.tsx
  - packages/web/test/bundle-data.test.ts
  - packages/web/test/proof-graph-render.test.ts
autonomous: true
requirements:
  - OBJ-01
  - OBJ-02
  - OBJ-03
  - REL-01
  - REL-02
  - REL-03
  - REL-04
must_haves:
  truths:
    - The gold paper and reduced fixtures emit first-class `section`, `proof`, `equation`, and `external_dependency` citation objects with stable IDs and source anchors.
    - Deterministic explicit edges and structural edges both serialize with coherent provenance and evidence detail.
    - Bundle-building heuristics and the current React graph view remain compatible with the richer graph without promoting section/proof bookkeeping to “main result” status.
  artifacts:
    - path: packages/core/src/ingestion/parsers/latex-parser.ts
      provides: Deterministic TeX object and relation population for Phase 2
    - path: packages/core/src/ingestion/parsers/markdown-parser.ts
      provides: Markdown compatibility with the expanded Phase 2 contract
    - path: packages/core/src/ingestion/bundle-builder.ts
      provides: Phase 2-safe main-result, summary, cluster, and attention heuristics
    - path: packages/core/test/fixtures/latex/gold-paper-relations/proof-equation.tex
      provides: Reduced deterministic fixture for proof, equation, section, and citation coverage
    - path: packages/web/src/components/proof-graph-page.tsx
      provides: Compatibility rendering for new node kinds plus provenance-aware edge detail
  key_links:
    - from: packages/core/src/ingestion/parsers/latex-parser.ts
      to: packages/core/src/types/node.ts
      via: stable IDs and anchors for new node kinds
      pattern: createNodeId
    - from: packages/core/src/ingestion/parsers/latex-parser.ts
      to: packages/core/src/types/edge.ts
      via: deterministic `contains`, `proves`, `uses_in_proof`, and `cites_external` edges
      pattern: provenance
    - from: packages/core/src/ingestion/bundle-builder.ts
      to: packages/web/src/lib/dashboard-model.ts
      via: serialized graph/index data consumed by the dashboard model
      pattern: main_results
---

<objective>
Populate the Phase 2 canonical contract on real parser outputs and keep the current web compatibility surfaces working with the richer graph.

Purpose: Turn the upgraded 0.2.0 contract into real deterministic objects, anchors, and relations for TeX and Markdown while keeping bundle heuristics and the React graph consumer coherent.
Output: Parser and bundle-builder updates, focused TeX fixtures, and web compatibility changes that make sections/proofs/equations/citations first-class without drifting into later-phase workflow work.
</objective>

<execution_context>
@/Users/hanzhicheng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hanzhicheng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@docs/architecture.md
@.planning/phases/01-gold-paper-tex-ingestion-hardening/01-01-SUMMARY.md
@.planning/phases/01-gold-paper-tex-ingestion-hardening/01-02-SUMMARY.md
@.planning/phases/02-canonical-objects-deterministic-relations/02-CONTEXT.md
@.planning/phases/02-canonical-objects-deterministic-relations/02-RESEARCH.md
@packages/core/src/ingestion/parsers/latex-parser.ts
@packages/core/src/ingestion/parsers/markdown-parser.ts
@packages/core/src/ingestion/bundle-builder.ts
@packages/core/src/ingestion/flatten/latex-flattener.ts
@packages/core/test/ingestion-pipeline.test.ts
@packages/core/test/markdown-parser.test.ts
@packages/core/test/gold-paper-ingestion.test.ts
@packages/web/src/lib/dashboard-model.ts
@packages/web/src/components/proof-graph-page.tsx
@packages/web/test/bundle-data.test.ts
@packages/web/test/proof-graph-render.test.ts

Locked decisions to honor in this phase:
- D-01: Preserve the canonical `manifest` / `graph` / `index` bundle architecture and keep the public contract additive at `0.2.0`.
- D-02: Deterministic parsing remains the trusted baseline; Phase 2 must not introduce agent-inferred semantics into the canonical artifact.
- D-03: Treat `external_dependency` as the Phase 2 citation object model; do not add a separate `citation` node kind unless implementation proves it is impossible.
- D-04: Source anchors must be the serialized `filePath`, `startLine`, and `endLine` fields already present on `MathNode`, not a new sidecar anchor system.
- D-05: Separate edge provenance from evidence so deterministic relations can carry both machine-filterable provenance and user-facing explanatory evidence.
- D-06: Keep Phase 2 scoped to canonical artifact quality and compatibility; no Phase 3 explorer workflow redesign and no Phase 4 enrichment storage.

<interfaces>
From `packages/core/src/types/node.ts` after Plan `02-01`:
```ts
export type MathNodeKind =
  | 'section'
  | 'definition'
  | 'theorem'
  | 'lemma'
  | 'proposition'
  | 'corollary'
  | 'assumption'
  | 'remark'
  | 'example'
  | 'conjecture'
  | 'notation'
  | 'proof'
  | 'equation'
  | 'external_dependency';
```

From `packages/core/src/types/edge.ts` after Plan `02-01`:
```ts
export type MathEdgeKind =
  | 'contains'
  | 'proves'
  | 'uses_in_proof'
  | 'extends'
  | 'generalizes'
  | 'specializes'
  | 'equivalent_to'
  | 'cites_external';
```

From `packages/core/src/ingestion/flatten/latex-flattener.ts`:
```ts
export interface FlattenLatexResult {
  flatTex: string;
  missingInputs: string[];
  missingBibs: string[];
  missingGraphics: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Populate anchored Phase 2 objects and deterministic relations on the TeX and Markdown ingestion paths</name>
  <files>packages/core/src/ingestion/parsers/latex-parser.ts, packages/core/src/ingestion/parsers/markdown-parser.ts, packages/core/test/fixtures/latex/gold-paper-relations/proof-equation.tex, packages/core/test/ingestion-pipeline.test.ts, packages/core/test/markdown-parser.test.ts</files>
  <read_first>
    .planning/phases/02-canonical-objects-deterministic-relations/02-CONTEXT.md
    .planning/phases/02-canonical-objects-deterministic-relations/02-RESEARCH.md
    packages/core/src/ingestion/parsers/latex-parser.ts
    packages/core/src/ingestion/parsers/markdown-parser.ts
    packages/core/src/ingestion/flatten/latex-flattener.ts
    packages/core/test/ingestion-pipeline.test.ts
    packages/core/test/markdown-parser.test.ts
    ref/papers/long_nalini/arXiv-2502.12268v2/main.tex
  </read_first>
  <behavior>
    - LaTeX parsing emits first-class `section`, `proof`, `equation`, and `external_dependency` citation nodes with stable IDs and deterministic anchors.
    - `\ref` and `\eqref` keep producing explicit deterministic dependency edges, citations stay on `external_dependency`, and structural `contains` / `proves` edges are added with `provenance=structural` and `evidence=structural`.
    - Citation objects use `proofStatus: 'external'` and anchor to the first deterministic `\cite{...}` occurrence rather than being unanchored placeholders.
    - Markdown stays compatible with the expanded Phase 2 contract, at minimum for section nodes, anchors, and the new provenance field on explicit edges.
  </behavior>
  <action>Per D-02 through D-05, extend parser output rather than inventing a second registry. In `packages/core/src/ingestion/parsers/latex-parser.ts`, keep theorem-like dependency semantics intact but add first-class `section` nodes from heading transitions, `proof` nodes from `\\begin{proof}` blocks, and `equation` nodes for labeled displayed equations or equation environments that are targets of `\\eqref`. Use the existing `% >>> BEGIN FILE:` / `% <<< END FILE:` flattener markers to derive `filePath`, `startLine`, and `endLine` directly in the parser under D-04; do not add a new source-map artifact unless the markers prove insufficient. Keep theorem-like or proved-statement nodes as the source for `uses_in_proof` and `cites_external` edges so later query surfaces remain theorem-centric, and add `contains` plus `proves` as separate structural links with explanatory `detail`/`metadata`. Keep `external_dependency` as the only citation node kind per D-03, set `proofStatus` to `external`, and use first-occurrence anchors and stable `sec0::ext:{slug}` IDs. In `packages/core/src/ingestion/parsers/markdown-parser.ts`, populate the new provenance field, emit `section` nodes and anchors from heading boundaries, and keep explicit reference edges schema-valid even if Markdown stays lighter than TeX in proof/equation support. Add `packages/core/test/fixtures/latex/gold-paper-relations/proof-equation.tex` as a reduced deterministic fixture that exercises section containment, proof attachment, labeled equations, citations, and source anchors without relying only on the gold paper.</action>
  <verify>
    <automated>npx vitest run packages/core/test/ingestion-pipeline.test.ts packages/core/test/markdown-parser.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Reduced parser tests prove stable IDs and anchors for section/proof/equation/citation objects, not just theorem-like statements.
    - Explicit and structural edges both serialize with coherent provenance/evidence fields and human-readable detail.
    - No parser change introduces a separate `citation` node kind or hides unresolved references behind silent fallbacks.
  </acceptance_criteria>
  <done>Both TeX and Markdown ingestion can emit Phase 2 objects and deterministic relations in a schema-valid way.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep bundle heuristics and the current React graph consumer coherent with the richer canonical graph</name>
  <files>packages/core/src/ingestion/bundle-builder.ts, packages/core/test/gold-paper-ingestion.test.ts, packages/web/src/lib/dashboard-model.ts, packages/web/src/components/proof-graph-page.tsx, packages/web/test/bundle-data.test.ts, packages/web/test/proof-graph-render.test.ts</files>
  <read_first>
    packages/core/src/ingestion/bundle-builder.ts
    packages/core/test/gold-paper-ingestion.test.ts
    packages/web/src/lib/dashboard-model.ts
    packages/web/src/components/proof-graph-page.tsx
    packages/web/test/bundle-data.test.ts
    packages/web/test/proof-graph-render.test.ts
    ref/papers/long_nalini/arXiv-2502.12268v2/main.tex
  </read_first>
  <behavior>
    - `buildBundleFromParsedDocument()` does not mistake `section`, `proof`, or `equation` nodes for headline results or noisy attention spikes unless explicitly warranted.
    - LaTeX `manifest.paper.versionNote` becomes deterministic enough that ACC-02 only needs to normalize `createdAt` and producer timestamps.
    - The current React graph view accepts the new node kinds and surfaces edge `provenance` alongside `evidence` without attempting later-phase explorer UX.
    - The gold-paper acceptance test asserts the presence of anchored Phase 2 objects and deterministic structural edges while remaining schema-valid.
  </behavior>
  <action>Per D-01, D-02, and D-06, keep bundle heuristics compatible with the richer graph instead of letting organizational nodes dominate downstream output. Update `packages/core/src/ingestion/bundle-builder.ts` so `selectMainNode()`, attention summaries, proof strategies, section summaries, and cluster descriptions continue to privilege theorem-like mathematical results while tolerating `section`, `proof`, `equation`, and citation nodes in the graph. Make `versionNote` deterministic by removing run timestamps from the value and limiting volatile run metadata to `manifest.createdAt` and `manifest.producer.*`, so ACC-02 can normalize a small explicit metadata set. Update `packages/core/test/gold-paper-ingestion.test.ts` to assert anchored Phase 2 nodes and provenance-aware deterministic edges on the real `long_nalini` paper. In `packages/web/src/lib/dashboard-model.ts` and `packages/web/src/components/proof-graph-page.tsx`, add compatibility for the new kinds and edge field while keeping the current evidence filter and overall page structure; show `provenance` in edge detail text and add stable color/button entries for `section`, `proof`, and `equation`, but do not design a new explorer workflow in this phase.</action>
  <verify>
    <automated>npx vitest run packages/core/test/gold-paper-ingestion.test.ts packages/web/test/bundle-data.test.ts packages/web/test/proof-graph-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The real gold-paper regression proves Phase 2 objects and anchors on actual TeX, not only on reduced fixtures.
    - Bundle-builder heuristics remain theorem-centric even though the graph now contains organizational section/proof nodes.
    - The current web graph consumer renders the richer bundle without crashing, hiding all new kinds, or dropping provenance detail.
  </acceptance_criteria>
  <done>Parser output, bundle heuristics, and the current web compatibility layer all understand the richer Phase 2 graph.</done>
</task>

</tasks>

<verification>
The parser-population wave is complete when real and reduced fixtures produce anchored Phase 2 objects plus provenance-aware deterministic relations, and the current web graph consumer stays compatible.

<automated>npx vitest run packages/core/test/ingestion-pipeline.test.ts packages/core/test/markdown-parser.test.ts packages/core/test/gold-paper-ingestion.test.ts packages/web/test/bundle-data.test.ts packages/web/test/proof-graph-render.test.ts</automated>
<automated>npm run typecheck --workspace @paperparser/core</automated>
<automated>npm run typecheck --workspace @paperparser/web</automated>
</verification>

<success_criteria>
Plan 02-02 is complete when the canonical bundle produced by TeX and Markdown ingestion contains first-class Phase 2 objects with anchors and deterministic relations, and the existing React graph surface can still consume that bundle coherently.
</success_criteria>

<output>
After completion, create `.planning/phases/02-canonical-objects-deterministic-relations/02-02-SUMMARY.md`
</output>
