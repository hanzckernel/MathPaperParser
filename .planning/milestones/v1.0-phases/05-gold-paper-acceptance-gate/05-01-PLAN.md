---
phase: 05-gold-paper-acceptance-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/cli/test/gold-paper-acceptance.test.ts
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ACC-01
must_haves:
  truths:
    - The acceptance gate proves the real user workflow on `long_nalini`, not only internal helper functions.
    - The workflow remains local-first and requires no manual graph editing.
    - The enrichment sidecar stays separate from the canonical bundle throughout the workflow.
  artifacts:
    - path: packages/cli/test/gold-paper-acceptance.test.ts
      provides: Real-paper end-to-end workflow regression
    - path: .planning/ROADMAP.md
      provides: Final roadmap completion state
    - path: .planning/STATE.md
      provides: Next-step handoff after milestone completion
  key_links:
    - from: packages/cli/test/gold-paper-acceptance.test.ts
      to: ref/papers/long_nalini/arXiv-2502.12268v2/main.tex
      via: representative-paper workflow
      pattern: long_nalini
---

<objective>
Close the milestone by proving the full accepted workflow on the representative heavy paper.

Purpose: turn the milestone success bar into an executable regression rather than a verbal claim.
Output: end-to-end gold-paper CLI acceptance test, verified docs/state updates, and a manual built-CLI confirmation.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-gold-paper-acceptance-gate/05-CONTEXT.md
@.planning/phases/05-gold-paper-acceptance-gate/05-RESEARCH.md
@.planning/phases/05-gold-paper-acceptance-gate/05-VALIDATION.md
@ref/papers/long_nalini/arXiv-2502.12268v2/main.tex
@packages/cli/src/index.ts
@packages/cli/test/

Locked decisions to honor in this plan:
- Use `long_nalini` as the only acceptance paper for this milestone.
- Assert workflow success and artifact separation, not brittle exact graph snapshots.
- Update roadmap/state only after the acceptance harness and built-CLI verification are both green.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the gold-paper end-to-end acceptance regression</name>
  <files>packages/cli/test/gold-paper-acceptance.test.ts</files>
  <action>Add a CLI-driven acceptance test that runs `analyze`, `enrich`, `validate`, and `export` on `ref/papers/long_nalini/arXiv-2502.12268v2/main.tex` using temporary store/output directories. Assert diagnostics are present, validation succeeds, the graph is non-trivial, `enrichment.json` exists separately from the canonical bundle, and the static export contains all expected files for local inspection.</action>
  <verify>
    <automated>npx vitest run packages/cli/test/gold-paper-acceptance.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The representative paper completes the full CLI workflow without manual edits.
    - The workflow yields diagnostics, canonical bundle files, and a populated enrichment sidecar.
    - The exported artifact contains the canonical files plus `data/enrichment.json`.
  </acceptance_criteria>
  <done>ACC-01 is backed by a concrete regression harness.</done>
</task>

</tasks>

<verification>
<automated>npx vitest run packages/cli/test/gold-paper-acceptance.test.ts</automated>
<automated>npm test</automated>
<automated>npm run typecheck</automated>
<automated>npm run build --workspace @paperparser/cli</automated>
</verification>

<success_criteria>
Plan 05-01 is complete when the real gold paper can be analyzed, enriched, validated, and exported end to end with no manual graph editing and the milestone docs reflect completion.
</success_criteria>

<output>
After completion, create `.planning/phases/05-gold-paper-acceptance-gate/05-01-SUMMARY.md`
</output>
