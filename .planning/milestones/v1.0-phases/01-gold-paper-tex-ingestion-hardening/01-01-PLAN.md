---
phase: 01-gold-paper-tex-ingestion-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/test/gold-paper-ingestion.test.ts
  - packages/core/test/fixtures/latex/gold-paper-regressions/front-matter.tex
  - packages/core/test/fixtures/latex/gold-paper-regressions/missing-input.tex
  - packages/core/test/fixtures/latex/gold-paper-regressions/unsupported-refs.tex
  - packages/core/test/ingestion-pipeline.test.ts
  - packages/core/test/latex-flattener.test.ts
  - packages/cli/test/analyze-command.test.ts
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
  - INGEST-03
must_haves:
  truths:
    - Wave 0 turns the known Phase 1 trust defects into named automated regressions before any source hardening starts.
    - The real acceptance target remains `ref/papers/long_nalini/arXiv-2502.12268v2/main.tex`, not `main.flat.tex` or a helper-only path.
    - Diagnostics persistence and unresolved reference handling are frozen as explicit expectations instead of remaining silent gaps.
  artifacts:
    - path: packages/core/test/gold-paper-ingestion.test.ts
      provides: Full-paper gold-paper regression on the real `main.tex` entrypoint
    - path: packages/core/test/fixtures/latex/gold-paper-regressions/front-matter.tex
      provides: Reduced fixture for optional-argument title and nested-brace author parsing
    - path: packages/core/test/fixtures/latex/gold-paper-regressions/missing-input.tex
      provides: Reduced fixture for explicit missing `\input` diagnostics
    - path: packages/core/test/fixtures/latex/gold-paper-regressions/unsupported-refs.tex
      provides: Reduced fixture for unresolved `\ref` / `\eqref` plus unsupported `\cref`
    - path: packages/cli/test/analyze-command.test.ts
      provides: Temp-store CLI regression that expects persisted diagnostics visibility
  key_links:
    - from: packages/core/test/gold-paper-ingestion.test.ts
      to: ref/papers/long_nalini/arXiv-2502.12268v2/main.tex
      via: analyzeDocumentPath()
      pattern: analyzeDocumentPath
    - from: packages/cli/test/analyze-command.test.ts
      to: packages/cli/src/index.ts
      via: runCli(['analyze', ...])
      pattern: runCli
    - from: packages/core/test/ingestion-pipeline.test.ts
      to: packages/core/src/ingestion/parsers/latex-parser.ts
      via: diagnostics assertions on warning codes
      pattern: diagnostics.warnings
---

<objective>
Create the Wave 0 regression harness for Phase 1.

Purpose: Freeze the actual gold-paper defects and required diagnostics on the current ingestion path before implementation so Plan 01-02 can harden the right behavior without rediscovery.
Output: New gold-paper acceptance tests, reduced regression fixtures, and a CLI store-persistence regression shaped by `01-VALIDATION.md`.
</objective>

<execution_context>
@/Users/hanzhicheng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hanzhicheng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/01-gold-paper-tex-ingestion-hardening/01-CONTEXT.md
@.planning/phases/01-gold-paper-tex-ingestion-hardening/01-RESEARCH.md
@.planning/phases/01-gold-paper-tex-ingestion-hardening/01-VALIDATION.md
@packages/core/test/ingestion-pipeline.test.ts
@packages/core/test/latex-flattener.test.ts
@packages/cli/test/analyze-command.test.ts
@packages/core/src/ingestion/pipeline.ts
@packages/core/src/types/pipeline.ts
@packages/cli/src/index.ts
@packages/cli/src/store.ts

Locked decisions to honor in this phase:
- D-01: The gold paper is `ref/papers/long_nalini/arXiv-2502.12268v2/main.tex`; do not replace it with `main.flat.tex`.
- D-02: Stay on `createDocumentInput -> analyzeDocumentPath -> flattenLatex -> parseLatexDocument` unless the real work proves replacement necessary.
- D-03: Keep Phase 1 scoped to ingestion hardening and diagnostics only; no explorer work, semantic inference, or broad schema redesign.
- D-04: The baseline already parses the gold paper; the known trust defects are broken title/author extraction, limited diagnostics, and diagnostics disappearing after store write.
- D-05: Use `01-VALIDATION.md` as the Wave 0 and acceptance-command contract.

<interfaces>
From `packages/core/src/ingestion/pipeline.ts`:
```ts
export function createDocumentInput(inputPath: string): DocumentInput;
export function analyzeDocumentPath(inputPath: string, options?: PipelineOptions): PipelineResult;
```

From `packages/core/src/types/pipeline.ts`:
```ts
export interface PipelineDiagnostics {
  schemaVersion: string;
  warnings: IngestionWarning[];
}

export interface PipelineResult extends PaperParserBundle {
  input: DocumentInput;
  diagnostics: PipelineDiagnostics;
}
```

From `packages/cli/src/index.ts`:
```ts
export interface CliIo {
  stdout: (line: string) => void;
  stderr: (line: string) => void;
  cwd?: string;
}

export function runCli(argv: string[], io?: CliIo): number;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the real-paper acceptance regression and CLI diagnostics-persistence regression</name>
  <files>packages/core/test/gold-paper-ingestion.test.ts, packages/cli/test/analyze-command.test.ts</files>
  <read_first>
    .planning/phases/01-gold-paper-tex-ingestion-hardening/01-CONTEXT.md
    .planning/phases/01-gold-paper-tex-ingestion-hardening/01-RESEARCH.md
    .planning/phases/01-gold-paper-tex-ingestion-hardening/01-VALIDATION.md
    packages/core/test/ingestion-pipeline.test.ts
    packages/cli/test/analyze-command.test.ts
    packages/core/src/ingestion/pipeline.ts
    packages/cli/src/index.ts
    packages/cli/src/store.ts
    ref/papers/long_nalini/arXiv-2502.12268v2/main.tex
  </read_first>
  <action>Per D-01 and D-05, create `packages/core/test/gold-paper-ingestion.test.ts` as the acceptance test on the real gold-paper `main.tex`. Assert the behavior Phase 1 must eventually satisfy: `analyzeDocumentPath()` returns `input.kind === 'latex'`, the manifest title contains `Friedman--Ramanujan functions`, authors contain both Nalini Anantharaman and Laura Monk, the graph still contains theorem-like content, and the warning codes do not include `missing_input`. In `packages/cli/test/analyze-command.test.ts`, add a temp-store regression that runs `runCli(['analyze', GOLD_TEX_PATH, '--store', storePath, '--paper', 'long-nalini'])`, then expects a persisted `diagnostics.json` sidecar and human-readable analyze output that mentions diagnostics when warnings remain. Keep these assertions pointed at the desired end state from D-04; do not weaken them to the current broken baseline, and do not use `main.flat.tex` or helper-only scripts.</action>
  <verify>
    <automated>npx vitest run packages/core/test/gold-paper-ingestion.test.ts packages/cli/test/analyze-command.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `packages/core/test/gold-paper-ingestion.test.ts` uses `ref/papers/long_nalini/arXiv-2502.12268v2/main.tex` directly and names the title/author trust defects from D-04 in executable assertions.
    - The CLI regression uses a real temp store and checks for `diagnostics.json`, not just in-memory diagnostics.
    - The targeted Vitest command currently fails because the baseline still loses title/author fidelity and stored diagnostics, not because of unrelated harness issues.
  </acceptance_criteria>
  <done>Wave 0 contains a reproducible red acceptance harness for the real paper and for diagnostics persistence across the store boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Add reduced regression fixtures for front matter, missing-include, and unresolved-reference diagnostics</name>
  <files>packages/core/test/fixtures/latex/gold-paper-regressions/front-matter.tex, packages/core/test/fixtures/latex/gold-paper-regressions/missing-input.tex, packages/core/test/fixtures/latex/gold-paper-regressions/unsupported-refs.tex, packages/core/test/ingestion-pipeline.test.ts, packages/core/test/latex-flattener.test.ts</files>
  <read_first>
    .planning/phases/01-gold-paper-tex-ingestion-hardening/01-CONTEXT.md
    .planning/phases/01-gold-paper-tex-ingestion-hardening/01-RESEARCH.md
    .planning/phases/01-gold-paper-tex-ingestion-hardening/01-VALIDATION.md
    packages/core/test/ingestion-pipeline.test.ts
    packages/core/test/latex-flattener.test.ts
    packages/core/src/ingestion/flatten/latex-flattener.ts
    packages/core/src/ingestion/parsers/latex-parser.ts
    ref/papers/long_nalini/arXiv-2502.12268v2/main.tex
  </read_first>
  <action>Per D-02, D-03, and D-05, derive small fixtures from the observed gold-paper failures instead of broadening scope. Create `front-matter.tex` with the exact failure shape: `\title[short]{long title}` plus nested-brace `\author{... \textsuperscript{1} ...}`. Create `missing-input.tex` with a required `\input{...}` that cannot be resolved so Phase 1 proves explicit include diagnostics rather than relying on implied coverage. Create `unsupported-refs.tex` with one theorem label, one unresolved `\eqref{missing:label}`, and at least one `\cref{...}` / `\Cref{...}` so Phase 1 can distinguish unresolved references from unsupported reference commands. Extend `packages/core/test/ingestion-pipeline.test.ts` to assert those warning codes explicitly and extend `packages/core/test/latex-flattener.test.ts` only where bibliography / asset-scanning behavior for the gold-paper shape needs to be frozen. Keep the existing small-project fixture assertion on `missing_bibliography` as the executable citation-source diagnostic for INGEST-03. Do not add semantic-edge assertions or canonical-schema changes per D-03.</action>
  <verify>
    <automated>npx vitest run packages/core/test/gold-paper-ingestion.test.ts packages/core/test/latex-flattener.test.ts packages/core/test/ingestion-pipeline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Reduced fixtures isolate the exact Phase 1 defects called out in D-04: brace-aware front matter, explicit missing-input diagnostics, and explicit unresolved/unsupported reference diagnostics.
    - `packages/core/test/ingestion-pipeline.test.ts` expects structured warning codes for missing required includes, unresolved references, and unsupported reference commands instead of silent omission.
    - The existing tracked latex-project fixture continues to prove explicit `missing_bibliography` diagnostics so INGEST-03 covers citation-source failures as well as include/reference failures.
    - The targeted Vitest command is red for the known gaps only, giving Plan 01-02 a narrow green target on the current pipeline.
  </acceptance_criteria>
  <done>Wave 0 contains narrow regressions for metadata parsing plus citation/include/reference diagnostics coverage, anchored to the real gold-paper behavior.</done>
</task>

</tasks>

<verification>
Wave 0 should finish with intentionally red regressions that expose only the known Phase 1 defects.

<automated>npx vitest run packages/core/test/gold-paper-ingestion.test.ts packages/cli/test/analyze-command.test.ts</automated>
Expected outcome: failing assertions tied to broken title/author extraction and missing stored diagnostics.

<automated>npx vitest run packages/core/test/gold-paper-ingestion.test.ts packages/core/test/latex-flattener.test.ts packages/core/test/ingestion-pipeline.test.ts</automated>
Expected outcome: failing assertions tied to missing unresolved-reference / unsupported-command diagnostics or bibliography classification gaps, with no unrelated harness failures.
</verification>

<success_criteria>
Plan 01-01 is complete when the repository has a reproducible Wave 0 harness for INGEST-01, INGEST-02, and INGEST-03, and every new failure or explicit diagnostic assertion maps back to a known Phase 1 trust defect or diagnostic contract from D-04/D-05.
</success_criteria>

<output>
After completion, create `.planning/phases/01-gold-paper-tex-ingestion-hardening/01-01-SUMMARY.md`
</output>
