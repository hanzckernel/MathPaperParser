---
phase: 01-gold-paper-tex-ingestion-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - packages/core/src/ingestion/parsers/latex-command-extractor.ts
  - packages/core/src/ingestion/parsers/latex-parser.ts
  - packages/core/src/ingestion/flatten/latex-flattener.ts
  - packages/cli/src/store.ts
  - packages/cli/src/index.ts
autonomous: true
requirements:
  - INGEST-01
  - INGEST-02
  - INGEST-03
must_haves:
  truths:
    - The real gold paper analyzes through the existing `createDocumentInput -> analyzeDocumentPath -> flattenLatex -> parseLatexDocument` path without requiring PDF input or ad hoc flatten-only shortcuts.
    - Stored output keeps trustworthy metadata and explicit diagnostics instead of dropping them after `analyze`.
    - Remaining unsupported constructs stay visible as structured diagnostics rather than disappearing silently.
  artifacts:
    - path: packages/core/src/ingestion/parsers/latex-command-extractor.ts
      provides: Brace-aware command argument extraction for Phase 1 front matter
    - path: packages/core/src/ingestion/parsers/latex-parser.ts
      provides: Gold-paper metadata repair plus unresolved / unsupported reference diagnostics
    - path: packages/core/src/ingestion/flatten/latex-flattener.ts
      provides: Gold-paper bibliography classification on the existing flattener path
    - path: packages/cli/src/store.ts
      provides: Persisted diagnostics sidecar written next to manifest/graph/index
    - path: packages/cli/src/index.ts
      provides: Human-readable analyze output that surfaces diagnostics after store write
  key_links:
    - from: packages/core/src/ingestion/pipeline.ts
      to: packages/core/src/ingestion/parsers/latex-parser.ts
      via: parseLatexDocument()
      pattern: parseLatexDocument
    - from: packages/cli/src/index.ts
      to: packages/cli/src/store.ts
      via: writeBundleToStore(bundle, ...)
      pattern: writeBundleToStore
    - from: packages/cli/src/store.ts
      to: packages/core/test/gold-paper-ingestion.test.ts
      via: persisted diagnostics.json sidecar
      pattern: diagnostics.json
---

<objective>
Harden the existing gold-paper TeX ingestion path and persist diagnostics without drifting into schema redesign.

Purpose: Make the already-working gold-paper parse trustworthy by fixing title/author extraction, explicit diagnostics, and post-store diagnostics visibility on the current Phase 1 pipeline.
Output: Narrow source changes in core and CLI that turn the Wave 0 regressions green and satisfy the accepted CLI analyze/validate commands.
</objective>

<execution_context>
@/Users/hanzhicheng/.codex/get-shit-done/workflows/execute-plan.md
@/Users/hanzhicheng/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@.planning/phases/01-gold-paper-tex-ingestion-hardening/01-CONTEXT.md
@.planning/phases/01-gold-paper-tex-ingestion-hardening/01-RESEARCH.md
@.planning/phases/01-gold-paper-tex-ingestion-hardening/01-VALIDATION.md
@packages/core/src/ingestion/pipeline.ts
@packages/core/src/ingestion/flatten/latex-flattener.ts
@packages/core/src/ingestion/parsers/latex-parser.ts
@packages/core/src/types/pipeline.ts
@packages/cli/src/store.ts
@packages/cli/src/index.ts
@packages/core/test/gold-paper-ingestion.test.ts
@packages/core/test/ingestion-pipeline.test.ts
@packages/core/test/latex-flattener.test.ts
@packages/cli/test/analyze-command.test.ts

Locked decisions to honor in this phase:
- D-01: The gold paper is `ref/papers/long_nalini/arXiv-2502.12268v2/main.tex`; do not replace it with `main.flat.tex`.
- D-02: Stay on `createDocumentInput -> analyzeDocumentPath -> flattenLatex -> parseLatexDocument`; targeted hardening first, replacement only if the work proves it necessary.
- D-03: Keep Phase 1 limited to ingestion hardening and diagnostics; no explorer work, semantic inference, or broad `manifest` / `graph` / `index` redesign.
- D-04: Fix the known trust defects: broken title extraction, broken author extraction, limited diagnostics, and diagnostics lost after store write.
- D-05: Use the acceptance commands already identified in `01-RESEARCH.md` and `01-VALIDATION.md`.

<interfaces>
From `packages/core/src/types/pipeline.ts`:
```ts
export interface IngestionWarning {
  code: string;
  severity: IngestionWarningSeverity;
  message: string;
  phase: PipelinePhaseName;
  sourcePath?: string;
  line?: number;
  metadata?: Record<string, unknown>;
}

export interface PipelineDiagnostics {
  schemaVersion: string;
  warnings: IngestionWarning[];
}
```

From `packages/core/src/ingestion/flatten/latex-flattener.ts`:
```ts
export interface FlattenLatexResult {
  flatTex: string;
  missingInputs: string[];
  missingBibs: string[];
  missingGraphics: string[];
}
```

From `packages/cli/src/store.ts`:
```ts
export interface StoredPaperState {
  paperId: string;
  storePath: string;
  bundleDir: string;
}

export function writeBundleToStore(bundle: PaperParserBundle, storePath: string, paperId: string): StoredPaperState;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Turn the gold-paper metadata and diagnostics regressions green on the existing core ingestion path</name>
  <files>packages/core/src/ingestion/parsers/latex-command-extractor.ts, packages/core/src/ingestion/parsers/latex-parser.ts, packages/core/src/ingestion/flatten/latex-flattener.ts</files>
  <read_first>
    .planning/phases/01-gold-paper-tex-ingestion-hardening/01-CONTEXT.md
    .planning/phases/01-gold-paper-tex-ingestion-hardening/01-RESEARCH.md
    .planning/phases/01-gold-paper-tex-ingestion-hardening/01-VALIDATION.md
    packages/core/test/gold-paper-ingestion.test.ts
    packages/core/test/ingestion-pipeline.test.ts
    packages/core/test/latex-flattener.test.ts
    packages/core/src/ingestion/flatten/latex-flattener.ts
    packages/core/src/ingestion/parsers/latex-parser.ts
    ref/papers/long_nalini/arXiv-2502.12268v2/main.tex
  </read_first>
  <behavior>
    - Gold-paper title extraction accepts `\title[short]{long}` and returns the full long title instead of `Untitled (auto-extracted)`.
    - Gold-paper author extraction tolerates nested braces like `\textsuperscript{1}` and returns both authors cleanly.
    - Unresolved `\ref` / `\eqref` and unsupported `\cref` / `\Cref` surface as structured warning codes instead of disappearing.
    - Existing `missing_input` and `missing_bibliography` diagnostics remain explicit after the hardening work so include and citation-source failures stay visible.
    - The gold paper no longer reports a false `missing_bibliography` warning solely because it ships `.bbl` rather than `.bib`.
  </behavior>
  <action>Per D-02 and D-04, keep the current flattener/parser pipeline and patch only the real failure modes. Add a small brace-aware extractor helper in `packages/core/src/ingestion/parsers/latex-command-extractor.ts` and use it from `packages/core/src/ingestion/parsers/latex-parser.ts` to replace the brittle `TITLE_RE` / `AUTHOR_RE` logic with parsing that supports an optional argument and nested braces for Phase 1 front matter only. In the same parser, extend diagnostics creation so unresolved labels from `\ref` / `\eqref` emit `unresolved_reference` warnings and `\cref` / `\Cref` emit a distinct `unsupported_reference_command` warning unless they are explicitly supported through the same label lookup. In `packages/core/src/ingestion/flatten/latex-flattener.ts`, classify the gold paper's bibliography presence correctly on the current path by recognizing `.bbl` as satisfying the local bibliography requirement or by downgrading the warning to a non-misleading explicit message. Do not add semantic inference, relation redesign, or a parser replacement per D-03.</action>
  <verify>
    <automated>npx vitest run packages/core/test/gold-paper-ingestion.test.ts packages/core/test/latex-flattener.test.ts packages/core/test/ingestion-pipeline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The Wave 0 core regressions go green without changing the acceptance target away from `main.tex`.
    - The gold-paper manifest title and authors match the real front matter closely enough to remove the known D-04 defects.
    - Diagnostics now include explicit include/reference warnings where the reduced fixtures demand them, while preserving the existing citation-source warning behavior on the tracked fixture.
    - The solution stays on the current pipeline and keeps the `manifest` / `graph` / `index` contract intact.
  </acceptance_criteria>
  <done>Core ingestion behavior is trustworthy on the gold paper and the targeted core test command passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist diagnostics after analyze and surface them in the CLI acceptance path</name>
  <files>packages/cli/src/store.ts, packages/cli/src/index.ts</files>
  <read_first>
    .planning/phases/01-gold-paper-tex-ingestion-hardening/01-CONTEXT.md
    .planning/phases/01-gold-paper-tex-ingestion-hardening/01-RESEARCH.md
    .planning/phases/01-gold-paper-tex-ingestion-hardening/01-VALIDATION.md
    packages/cli/test/analyze-command.test.ts
    packages/cli/src/store.ts
    packages/cli/src/index.ts
    packages/core/src/types/pipeline.ts
    packages/core/test/gold-paper-ingestion.test.ts
  </read_first>
  <behavior>
    - `paperparser analyze ...` keeps working on the gold paper and writes `diagnostics.json` next to `manifest.json`, `graph.json`, and `index.json`.
    - The persisted diagnostics sidecar preserves warning codes, severity, message, phase, and source metadata without changing the canonical bundle schema.
    - CLI analyze output remains human-usable for the manual check in `01-VALIDATION.md` by surfacing whether diagnostics were written and what warnings remain.
  </behavior>
  <action>Per D-04 and D-05, persist diagnostics across the store boundary without redesigning the bundle schema. Update `packages/cli/src/store.ts` so `writeBundleToStore()` detects an incoming `PipelineResult` / diagnostics-bearing bundle via a narrow type guard and writes `diagnostics.json` beside the existing bundle files, while leaving the `manifest.json` / `graph.json` / `index.json` contract untouched. In `packages/cli/src/index.ts`, extend `runAnalyze()` so success output still prints `paper_id` and `bundle_dir` but also reports the diagnostics sidecar path or warning summary when warnings exist, making the manual acceptance check understandable. Keep the change scoped to the current CLI/store path; do not add API, MCP, or schema redesign work unless required by the same shared store helper.</action>
  <verify>
    <automated>npx vitest run packages/cli/test/analyze-command.test.ts packages/core/test/gold-paper-ingestion.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The CLI regression passes with a real temp store and a real `diagnostics.json` file on disk.
    - The persisted diagnostics sidecar is additive to the store layout rather than a schema mutation of `manifest.json`, `graph.json`, or `index.json`.
    - Analyze output gives a user enough information to tell whether the parse succeeded cleanly or with explicit warnings, as required by `01-VALIDATION.md`.
  </acceptance_criteria>
  <done>Diagnostics survive store write and the CLI acceptance path exposes them clearly enough for both automated and manual validation.</done>
</task>

</tasks>

<verification>
Use the existing validation strategy and acceptance commands from D-05 after both tasks pass.

<automated>npx vitest run packages/core/test/gold-paper-ingestion.test.ts packages/core/test/latex-flattener.test.ts packages/core/test/ingestion-pipeline.test.ts packages/cli/test/analyze-command.test.ts</automated>
<automated>npm run build --workspace @paperparser/cli</automated>
<automated>node packages/cli/dist/index.js analyze ref/papers/long_nalini/arXiv-2502.12268v2/main.tex --store /tmp/paperparser-phase1-acceptance --paper long-nalini</automated>
<automated>node packages/cli/dist/index.js analyze ref/papers/long_nalini/arXiv-2502.12268v2/main.tex --store /tmp/paperparser-phase1-acceptance-rerun --paper long-nalini</automated>
<automated>node packages/cli/dist/index.js validate --store /tmp/paperparser-phase1-acceptance --paper long-nalini --json</automated>
<automated>node -e "const fs=require('node:fs'); const diagnostics=JSON.parse(fs.readFileSync('/tmp/paperparser-phase1-acceptance/long-nalini/diagnostics.json','utf8')); if(!Array.isArray(diagnostics.warnings)) throw new Error('diagnostics.json missing warnings array'); if(diagnostics.warnings.some((warning)=>warning.code==='missing_input')) throw new Error('unexpected missing_input warning for gold paper'); console.log(JSON.stringify({warningCount: diagnostics.warnings.length, codes: [...new Set(diagnostics.warnings.map((warning)=>warning.code))].sort()}, null, 2));"</automated>
<automated>node -e "const fs=require('node:fs'); const path=require('node:path'); const firstBase='/tmp/paperparser-phase1-acceptance/long-nalini'; const secondBase='/tmp/paperparser-phase1-acceptance-rerun/long-nalini'; const firstManifest=JSON.parse(fs.readFileSync(path.join(firstBase,'manifest.json'),'utf8')); const secondManifest=JSON.parse(fs.readFileSync(path.join(secondBase,'manifest.json'),'utf8')); const firstDiagnostics=JSON.parse(fs.readFileSync(path.join(firstBase,'diagnostics.json'),'utf8')); const secondDiagnostics=JSON.parse(fs.readFileSync(path.join(secondBase,'diagnostics.json'),'utf8')); const stableManifestFields={title:firstManifest.paper.title,authors:firstManifest.paper.authors,nodeCount:JSON.parse(fs.readFileSync(path.join(firstBase,'graph.json'),'utf8')).nodes.length,edgeCount:JSON.parse(fs.readFileSync(path.join(firstBase,'graph.json'),'utf8')).edges.length}; if (firstManifest.paper.title !== secondManifest.paper.title) throw new Error('manifest titles differ across deterministic reruns'); if (JSON.stringify(firstManifest.paper.authors)!==JSON.stringify(secondManifest.paper.authors)) throw new Error('manifest authors differ across deterministic reruns'); const firstCodes=[...new Set(firstDiagnostics.warnings.map((warning)=>warning.code))].sort(); const secondCodes=[...new Set(secondDiagnostics.warnings.map((warning)=>warning.code))].sort(); if (JSON.stringify(firstCodes)!==JSON.stringify(secondCodes)) throw new Error('diagnostic codes differ across deterministic reruns'); console.log(JSON.stringify({stableManifestFields,warningCodes:firstCodes}, null, 2));"</automated>
<automated>npm test</automated>
<automated>npm run typecheck</automated>
</verification>

<success_criteria>
Plan 01-02 is complete when all Wave 0 regressions are green, the gold paper analyzes and validates through the existing CLI path, the stored output includes `diagnostics.json`, and no work drifted into explorer, semantic inference, or schema redesign.
</success_criteria>

<output>
After completion, create `.planning/phases/01-gold-paper-tex-ingestion-hardening/01-02-SUMMARY.md`
</output>
