---
phase: 06-searchable-bundle-index-explorer-navigation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/types/search.ts
  - packages/core/src/search/keyword-search.ts
  - packages/core/src/services/bundle-query-service.ts
  - packages/web/src/lib/api-client.ts
  - packages/web/src/App.tsx
  - packages/web/src/components/data-controls.tsx
  - packages/core/test/query-service.test.ts
  - packages/cli/test/read-commands.test.ts
  - packages/cli/test/serve-app.test.ts
  - packages/mcp/test/server.test.ts
  - packages/web/test/proof-graph-render.test.ts
autonomous: true
requirements:
  - SEARCH-01
  - SEARCH-02
  - SEARCH-03
  - SEARCH-04
must_haves:
  truths:
    - Search stays paper-local and aligned with the existing stored-paper bundle, not a separate backend.
    - The shared search contract becomes rich enough for human disambiguation without breaking current CLI/API/MCP usage.
    - The web app exposes explicit ranked results and opens the selected node in the existing explorer route.
  artifacts:
    - path: packages/core/src/types/search.ts
      provides: Shared search result contract with UI-usable metadata
    - path: packages/web/src/App.tsx
      provides: Search UI and explorer navigation behavior
    - path: packages/core/test/query-service.test.ts
      provides: Core search regression coverage
  key_links:
    - from: packages/core/src/search/keyword-search.ts
      to: packages/core/test/query-service.test.ts
      via: richer match scoring and result metadata
      pattern: theorem number and object identity search
    - from: packages/web/src/App.tsx
      to: packages/web/test/proof-graph-render.test.ts
      via: rendered search result list and explorer navigation affordance
      pattern: Open in Explorer
---

<objective>
Finish Phase 6 by turning existing paper-local search into a shared, human-usable search workflow with direct explorer navigation.

Purpose: satisfy the `v1.1` search requirement without adding a second search subsystem.
Output: richer shared search results plus a dashboard search surface that works in static and API modes.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-searchable-bundle-index-explorer-navigation/06-CONTEXT.md
@.planning/phases/06-searchable-bundle-index-explorer-navigation/06-RESEARCH.md
@.planning/phases/06-searchable-bundle-index-explorer-navigation/06-UI-SPEC.md
@packages/core/src/types/search.ts
@packages/core/src/search/keyword-search.ts
@packages/core/src/services/bundle-query-service.ts
@packages/web/src/App.tsx
@packages/web/src/lib/api-client.ts
@packages/web/src/components/data-controls.tsx
@packages/web/src/components/proof-graph-page.tsx
@packages/core/test/query-service.test.ts
@packages/cli/test/read-commands.test.ts
@packages/cli/test/serve-app.test.ts
@packages/mcp/test/server.test.ts
@packages/web/test/proof-graph-render.test.ts

Locked decisions to honor in this phase:
- Reuse the shared core search service instead of adding a new dependency or storage layer.
- Keep search paper-local for `v1.1`; corpus-wide search stays deferred.
- Keep explorer navigation inside the existing app shell and selected-node flow.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the shared search contract for human-usable disambiguation</name>
  <files>packages/core/src/types/search.ts, packages/core/src/search/keyword-search.ts, packages/core/src/services/bundle-query-service.ts, packages/core/test/query-service.test.ts, packages/cli/test/read-commands.test.ts, packages/cli/test/serve-app.test.ts, packages/mcp/test/server.test.ts</files>
  <action>Additive update to search so results include enough metadata for UI and machine consumers to disambiguate hits. Expand matching over object identity fields such as number, kind, and `latexLabel` where available. Keep the existing service and routes intact while updating tests to prove the richer result shape.</action>
  <verify>
    <automated>npx vitest run packages/core/test/query-service.test.ts packages/cli/test/read-commands.test.ts packages/cli/test/serve-app.test.ts packages/mcp/test/server.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Query results expose display metadata beyond bare node IDs.
    - A search for an object by number or object identity field succeeds in core tests.
    - CLI, API, and MCP keep returning the same paper-local results with the richer shape.
  </acceptance_criteria>
  <done>The shared search contract is ready for a human-facing UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add explorer-linked search to the dashboard shell</name>
  <files>packages/web/src/lib/api-client.ts, packages/web/src/App.tsx, packages/web/src/components/data-controls.tsx, packages/web/test/proof-graph-render.test.ts</files>
  <action>Add a paper-local search UI to the existing web shell. In static mode, use the loaded bundle through the shared core service. In API mode, use the existing paper query endpoint. Show ranked results with kind, label, and paper-local identity, then navigate to the existing explorer route with the selected node focused. Keep the graph-page filter as a separate graph-local control.</action>
  <verify>
    <automated>npx vitest run packages/web/test/proof-graph-render.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The dashboard renders a search result list separate from the graph-only filter.
    - Search results expose enough context to distinguish nearby hits.
    - Selecting a result opens the explorer route with the node selected.
  </acceptance_criteria>
  <done>The web layer satisfies SEARCH-01 through SEARCH-04 on the existing shell.</done>
</task>

</tasks>

<verification>
<automated>npx vitest run packages/core/test/query-service.test.ts packages/cli/test/read-commands.test.ts packages/cli/test/serve-app.test.ts packages/mcp/test/server.test.ts packages/web/test/proof-graph-render.test.ts</automated>
<automated>npm test</automated>
<automated>npm run typecheck</automated>
</verification>

<success_criteria>
Plan 06-01 is complete when the product exposes one paper-local search contract across its shipped surfaces and the dashboard can search a paper and open the chosen result in the explorer.
</success_criteria>

<output>
After completion, create `.planning/phases/06-searchable-bundle-index-explorer-navigation/06-01-SUMMARY.md`
</output>
