---
phase: 07-tex-hardening-diagnostic-reduction
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/ingestion/parsers/latex-parser.ts
  - packages/core/test/ingestion-pipeline.test.ts
  - packages/core/test/gold-paper-ingestion.test.ts
  - packages/core/test/fixtures/latex/gold-paper-regressions/nested-envs.tex
autonomous: true
requirements:
  - HARD-01
  - HARD-02
  - HARD-03
  - HARD-04
  - HARD-05
must_haves:
  truths:
    - The deterministic parser must cover the measured corpus gaps instead of hiding them behind enrichment.
    - Nested and same-line supported environments are the main parser failure mode and must be fixed before adding new output forms.
    - Residual unsupported patterns stay explicit; this phase does not silently invent targets.
  artifacts:
    - path: packages/core/src/ingestion/parsers/latex-parser.ts
      provides: Broader deterministic LaTeX coverage for the milestone corpus
    - path: packages/core/test/fixtures/latex/gold-paper-regressions/nested-envs.tex
      provides: Stable regression fixture for nested, same-line, and labeled-heading behavior
    - path: packages/core/test/gold-paper-ingestion.test.ts
      provides: Real-corpus hardening guardrails on unresolved diagnostics
  key_links:
    - from: packages/core/src/ingestion/parsers/latex-parser.ts
      to: packages/core/test/ingestion-pipeline.test.ts
      via: nested supported environment extraction and hierarchical heading labels
      pattern: labeled `align`, `eqnarray`, `lemma`, and subsection refs resolve
    - from: packages/core/src/ingestion/parsers/latex-parser.ts
      to: packages/core/test/gold-paper-ingestion.test.ts
      via: lower unresolved-reference counts on the accepted corpus
      pattern: `long_nalini`, `medium_Mueller.flat.tex`, `short_Petri.tex`
---

<objective>
Finish Phase 7 by hardening deterministic LaTeX parsing around the real unresolved-reference patterns measured in the milestone corpus.

Purpose: make the parser trustworthy on the accepted papers before adding multi-paper corpus features.
Output: broader deterministic coverage, tighter regression fixtures, and a materially reduced unresolved-reference baseline.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-tex-hardening-diagnostic-reduction/07-CONTEXT.md
@.planning/phases/07-tex-hardening-diagnostic-reduction/07-RESEARCH.md
@packages/core/src/ingestion/parsers/latex-parser.ts
@packages/core/test/ingestion-pipeline.test.ts
@packages/core/test/gold-paper-ingestion.test.ts
@packages/cli/test/gold-paper-acceptance.test.ts
@ref/papers/long_nalini/arXiv-2502.12268v2/main.tex
@ref/papers/medium_Mueller.flat.tex
@ref/papers/short_Petri.tex

Locked decisions to honor in this phase:
- Fix the measured deterministic parser gaps first: nested supported environments, same-line supported environments, broader equation-like envs, and labeled subsection headings.
- Keep missing bibliography and missing graphics diagnostics explicit.
- Do not introduce figure nodes unless the narrower hardening slice cannot meet the measured reduction target.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the Phase 7 parser failures in stable regression tests</name>
  <files>packages/core/test/fixtures/latex/gold-paper-regressions/nested-envs.tex, packages/core/test/ingestion-pipeline.test.ts</files>
  <action>Add a focused LaTeX fixture that reproduces the Phase 7 gaps: nested theorem/equation environments, same-line equation-like environments, and labeled subsection targets. Write failing tests that assert those labels resolve deterministically and do not emit unresolved-reference diagnostics.</action>
  <verify>
    <automated>npx vitest run packages/core/test/ingestion-pipeline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The new regression fixture fails on the current parser for the intended reason.
    - Tests assert exact resolved labels for nested equation-like and subsection targets.
    - The fixture does not depend on agent enrichment.
  </acceptance_criteria>
  <done>The parser hardening target is locked in executable tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Harden the deterministic LaTeX parser for the measured corpus slice</name>
  <files>packages/core/src/ingestion/parsers/latex-parser.ts, packages/core/test/ingestion-pipeline.test.ts</files>
  <action>Extend the parser to recognize the measured equation-like environments, emit nested supported nodes deterministically, handle same-line begin/end blocks, and create labeled subsection / subsubsection section nodes without destabilizing downstream top-level section grouping.</action>
  <verify>
    <automated>npx vitest run packages/core/test/ingestion-pipeline.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Nested labeled equations and theorem-like blocks become graph nodes and ref targets.
    - Same-line supported environments resolve labels correctly.
    - Labeled subsection and subsubsection headings resolve as section nodes.
    - Residual unsupported constructs still produce explicit diagnostics.
  </acceptance_criteria>
  <done>The parser resolves the measured deterministic gaps without a schema rewrite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Lock the corpus-level hardening outcome</name>
  <files>packages/core/test/gold-paper-ingestion.test.ts, packages/cli/test/gold-paper-acceptance.test.ts</files>
  <action>Add real-corpus guardrails proving that `medium_Mueller.flat.tex` and `short_Petri.tex` parse without unresolved references, and that `long_nalini` drops well below the old unresolved baseline while keeping the deferred residual explicit.</action>
  <verify>
    <automated>npx vitest run packages/core/test/gold-paper-ingestion.test.ts packages/cli/test/gold-paper-acceptance.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `medium_Mueller.flat.tex` emits zero unresolved references.
    - `short_Petri.tex` emits zero unresolved references while retaining missing-asset diagnostics.
    - `long_nalini` unresolved references are materially lower than the pre-phase baseline and the residual class is explicit.
  </acceptance_criteria>
  <done>Phase 7 has measurable proof on the accepted corpus.</done>
</task>

</tasks>

<verification>
<automated>npx vitest run packages/core/test/ingestion-pipeline.test.ts packages/core/test/gold-paper-ingestion.test.ts packages/cli/test/gold-paper-acceptance.test.ts</automated>
<automated>npm test</automated>
<automated>npm run typecheck</automated>
</verification>

<success_criteria>
Plan 07-01 is complete when the accepted corpus parses deterministically with materially fewer unresolved references, no unresolved references on the two secondary fixtures, and only explicit residual diagnostics left on the gold paper.
</success_criteria>

<output>
After completion, create `.planning/phases/07-tex-hardening-diagnostic-reduction/07-01-SUMMARY.md`
</output>
