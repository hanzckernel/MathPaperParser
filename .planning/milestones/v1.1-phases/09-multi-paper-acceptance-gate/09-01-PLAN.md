---
phase: 09-multi-paper-acceptance-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/services/corpus-query-service.ts
  - packages/cli/test/gold-paper-acceptance.test.ts
  - packages/cli/test/serve-app.test.ts
autonomous: true
requirements:
  - ACC-01
  - ACC-02
  - ACC-03
must_haves:
  truths:
    - Phase 9 proves behavior on the accepted real corpus; it does not introduce a new product surface.
    - Acceptance must cover the milestone workflow explicitly: validate, search, inspect, and corpus navigation.
    - If the corpus matcher needs cleanup, keep it narrow and deterministic.
  artifacts:
    - path: packages/cli/test/gold-paper-acceptance.test.ts
      provides: Milestone-level CLI workflow proof on the real corpus
    - path: packages/cli/test/serve-app.test.ts
      provides: API-backed corpus workflow proof on the same store
    - path: packages/core/src/services/corpus-query-service.ts
      provides: Final matcher hardening if acceptance exposes weak evidence terms
  key_links:
    - from: packages/cli/test/gold-paper-acceptance.test.ts
      to: ref/papers/long_nalini/arXiv-2502.12268v2/main.tex
      via: accepted search and inspect workflow
      pattern: `analyze -> validate -> query -> context`
    - from: packages/cli/test/serve-app.test.ts
      to: ref/papers/medium_Mueller.flat.tex
      via: shared local store
      pattern: corpus listing and API inspection on the real three-paper set
    - from: packages/core/src/services/corpus-query-service.ts
      to: packages/cli/test/serve-app.test.ts
      via: explainable cross-paper evidence
      pattern: real-corpus related results carry meaningful evidence terms
---

<objective>
Finish `v1.1` by proving the accepted local workflow on the full three-paper corpus and locking that proof into automated acceptance tests.

Purpose: close the milestone on real behavior rather than phase-local confidence.
Output: a stable multi-paper acceptance gate, plus only the narrow matcher cleanup needed to keep the proof explainable.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-multi-paper-acceptance-gate/09-CONTEXT.md
@.planning/phases/09-multi-paper-acceptance-gate/09-RESEARCH.md
@packages/core/src/services/corpus-query-service.ts
@packages/cli/test/gold-paper-acceptance.test.ts
@packages/cli/test/serve-app.test.ts
@ref/papers/long_nalini/arXiv-2502.12268v2/main.tex
@ref/papers/medium_Mueller.flat.tex
@ref/papers/short_Petri.tex

Locked decisions to honor in this phase:
- Prove the workflow on the accepted real corpus, not only on synthetic fixtures.
- Keep any final matcher cleanup narrow, deterministic, and explainability-focused.
- Do not reopen schema scope or add new surfaces in the milestone closeout.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the real-corpus acceptance gate</name>
  <files>packages/cli/test/gold-paper-acceptance.test.ts, packages/cli/test/serve-app.test.ts</files>
  <action>Add failing acceptance tests that load the real three-paper corpus into one store, validate each paper, search each paper with stable real queries, inspect selected nodes, and verify corpus listing plus cross-paper navigation on the same store.</action>
  <verify>
    <automated>npx vitest run packages/cli/test/gold-paper-acceptance.test.ts packages/cli/test/serve-app.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `long_nalini` is covered for `analyze -> validate -> search -> inspect`.
    - The shared store acceptance test includes `long_nalini`, `medium_Mueller.flat.tex`, and `short_Petri.tex`.
    - Cross-paper acceptance asserts explainable evidence, not just non-empty results.
  </acceptance_criteria>
  <done>The milestone proof is locked in executable tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tighten matcher evidence only if acceptance requires it</name>
  <files>packages/core/src/services/corpus-query-service.ts, packages/cli/test/gold-paper-acceptance.test.ts, packages/cli/test/serve-app.test.ts</files>
  <action>If the real-corpus acceptance tests expose weak or noisy evidence terms, make a narrow deterministic cleanup to token normalization and stopword filtering so the acceptance proof uses reasonable evidence.</action>
  <verify>
    <automated>npx vitest run packages/cli/test/gold-paper-acceptance.test.ts packages/cli/test/serve-app.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Acceptance relies on evidence a mathematician can inspect without guessing.
    - No new schema or surface is introduced to pass the gate.
  </acceptance_criteria>
  <done>The acceptance proof uses explainable real-corpus evidence.</done>
</task>

</tasks>

<verification>
<automated>npx vitest run packages/cli/test/gold-paper-acceptance.test.ts packages/cli/test/serve-app.test.ts</automated>
<automated>npm test</automated>
<automated>npm run typecheck</automated>
</verification>

<success_criteria>
Plan 09-01 is complete when the repo proves `analyze -> validate -> search -> inspect` on `long_nalini`, proves the same workflow on a shared local corpus of all three accepted papers, and the full verification suite remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/09-multi-paper-acceptance-gate/09-01-SUMMARY.md`
</output>
