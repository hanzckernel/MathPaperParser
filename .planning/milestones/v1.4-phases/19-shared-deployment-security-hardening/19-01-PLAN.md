---
phase: 19-shared-deployment-security-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/cloudrun/deploy.sh
  - deploy/cloudrun/grant-invoker.sh
  - deploy/cloudrun/README.md
  - packages/cli/test/cloud-run-security-contract.test.ts
  - README.md
  - docs/deployment_readiness.md
autonomous: true
requirements:
  - ACCESS-01
  - AUTH-01
  - AUTH-02
must_haves:
  truths:
    - The supported Cloud Run shared-deployment path must remain authenticated by default.
    - Repo-owned deploy/access artifacts must not silently drift into public access.
    - The documented access model and the scripted config path must match.
  artifacts:
    - path: deploy/cloudrun/deploy.sh
      provides: explicit Cloud Run deploy path with IAM auth still enabled
    - path: deploy/cloudrun/grant-invoker.sh
      provides: explicit named-principal access grants with public principals rejected
    - path: packages/cli/test/cloud-run-security-contract.test.ts
      provides: local proof that deploy/access scripts preserve the secure path
    - path: deploy/cloudrun/README.md
      provides: explicit supported shared-deployment access model
  key_links:
    - from: deploy/cloudrun/deploy.sh
      to: deploy/cloudrun/grant-invoker.sh
      via: shared Cloud Run security model
      pattern: deploy keeps IAM auth enabled and access grants stay explicit
    - from: deploy/cloudrun/README.md
      to: docs/deployment_readiness.md
      via: shared operator contract
      pattern: the documented model is direct authenticated Cloud Run access and public access is unsupported
---

<objective>
Finish Phase 19 by making the supported Cloud Run path explicitly authenticated and by adding repo-backed guardrails against accidental public exposure.

Purpose: turn "Cloud Run should probably stay private" into repo-owned deploy/access artifacts and tests.
Output: authenticated deploy script, explicit invoker-grant helper, access-model docs, and local contract proof.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/19-shared-deployment-security-hardening/19-CONTEXT.md
@.planning/phases/19-shared-deployment-security-hardening/19-RESEARCH.md
@Dockerfile
@README.md
@docs/deployment_readiness.md

Locked decisions to honor in this phase:
- Use direct authenticated Cloud Run service access as the first supported shared model.
- Keep the Cloud Run Invoker IAM check enabled.
- Reject public principals such as `allUsers` and `allAuthenticatedUsers` in repo-owned access helpers.
- Defer load balancers, IAP, custom domains, and disabled default URLs.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Capture the secure deploy/access contract in failing mocked-script tests</name>
  <files>packages/cli/test/cloud-run-security-contract.test.ts</files>
  <action>Add mocked `gcloud` regressions that require the deploy script to keep `--invoker-iam-check` enabled and require the invoker-grant helper to reject public principals before the scripts exist.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-run-security-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - At least one new security-contract regression fails before implementation.
    - Tests prove public principals are rejected locally.
    - Tests prove the deploy path never opts into public access flags.
  </acceptance_criteria>
  <done>The supported Cloud Run access model is executable before implementation begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement authenticated Cloud Run deploy/access helpers</name>
  <files>deploy/cloudrun/deploy.sh, deploy/cloudrun/grant-invoker.sh, deploy/cloudrun/README.md</files>
  <action>Add repo-owned Cloud Run deploy/access scripts that preserve IAM authentication, set explicit ingress, and reject unsupported public principals, then document the supported shared-deployment model.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-run-security-contract.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - The deploy helper keeps authenticated access enabled explicitly.
    - The invoker helper grants only `roles/run.invoker` to named principals and rejects public members.
    - The docs describe the same supported model the scripts implement.
  </acceptance_criteria>
  <done>The repo contains an operator-visible secure shared-deployment path instead of a console-only convention.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Re-prove the security contract and align readiness docs</name>
  <files>packages/cli/test/cloud-run-security-contract.test.ts, README.md, docs/deployment_readiness.md</files>
  <action>Re-run the mocked security-contract tests and typecheck, then update the readiness docs so they no longer claim access control is undefined while still keeping remaining blockers explicit.</action>
  <verify>
    <automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-run-security-contract.test.ts</automated>
    <automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The local proof shows the secure path is codified and public drift is rejected.
    - Docs describe shared deployment as authenticated by default, not open-by-default.
    - Remaining blockers such as persistence and deeper upload hardening stay explicit.
  </acceptance_criteria>
  <done>The security hardening is both implemented and accurately documented.</done>
</task>

</tasks>

<verification>
<automated>PATH=/opt/homebrew/bin:$PATH npm test -- packages/cli/test/cloud-run-security-contract.test.ts</automated>
<automated>PATH=/opt/homebrew/bin:$PATH npm run typecheck</automated>
</verification>

<success_criteria>
Plan 19-01 is complete when the repo ships Cloud Run deploy/access helpers that preserve authenticated access by default, reject unsupported public grants, document the same supported shared-deployment model, and prove the contract locally through mocked-script tests plus typecheck.
</success_criteria>

<output>
After completion, create `.planning/phases/19-shared-deployment-security-hardening/19-01-SUMMARY.md`
</output>
